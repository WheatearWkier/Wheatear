# 机甲小子 UI 布局参考分析

## 0. 分析目的

本文档记录对本地目录 `D:/games/jijia` 中《机甲小子 / 机甲旋风》UI SWF 资源的布局解析结果。

目标不是复制原游戏素材、代码或界面，而是提炼它在战斗 HUD、装备背包、技能面板、角色属性、强化和仓库页面上的信息摆放规律，用来指导 Wheatear 竖切 UI 的下一轮打磨。

## 1. 分析方法

资源大多是 Adobe AIR / Flash 的 `CWS` 压缩 SWF。解析方式如下：

- 解压 SWF 数据。
- 读取舞台尺寸和帧率。
- 读取 `ExportAssets` / `SymbolClass`，找到导出的主面板类。
- 递归读取 `PlaceObject` / `PlaceObject2` 显示列表。
- 解析每个实例的矩阵，得到坐标、缩放、层级和实例名。

结论：这些 UI 的舞台大多是 `960x600`，左上角为原点。下面表格中的坐标均按这个坐标系记录。

部分控件出现负坐标，不代表错误，通常是遮罩、滑动面板、运行时弹出层或鼠标跟随提示框。

## 2. 战斗 HUD

参考资源：

| 文件 | 导出类 | 舞台 |
| --- | --- | --- |
| `j_wanjiajiemianv443.swf` | `zhujiemian` | `960x600` |
| `j_bossxuetiaov43.swf` | `bossxuetiao` | `960x600` |

### 2.1 玩家状态区

玩家状态集中在左上角，不压住底部操作区：

| 元件 | 坐标 | 作用 |
| --- | --- | --- |
| `renwuxuetiao` | `(107.5, 31.4)` | 玩家生命条 |
| `xuetiaoshuzi` | `(183.5, 32.4)` | 生命数字 |
| `renwunengliangtiao` | `(100.8, 49.9)` | 能量条 |
| `nengliangtiaoshuz` | `(183.0, 50.8)` | 能量数字 |
| `jingyantiao` | `(118.6, 67.8)` | 经验条 |
| `jingyantiaoshuzi` | `(156.0, 67.3)` | 经验数字 |
| `tilitiao` | `(117.7, 80.5)` | 体力条 |
| `tilitiaoshuzi` | `(199.9, 79.3)` | 体力数字 |

可迁移规则：

- Wheatear 战斗 HUD 左上角只放主角生命、魔剑能量、断限槽和必要数字。
- 断限槽应独立成醒目的条，不和生命条混在一起。
- 条形 UI 不宜太高，避免遮挡俯视横板战斗空间。

### 2.2 底部道具槽与技能槽

底部是最值得参考的部分。它把道具、技能、系统按钮分成三段：

| 区域 | 元件 | 坐标规律 |
| --- | --- | --- |
| 左侧道具槽 | `caotubiaoa-d` | `(11.5, 547.0)`、`(71.9, 547.0)`、`(130.9, 547.0)`、`(193.8, 547.0)` |
| 道具选中框 | `caoxuandingkuanga-d` | `(16.6, 551.2)`、`(75.2, 551.2)`、`(133.5, 551.2)`、`(192.4, 551.2)` |
| 技能框 | `jinnengkuang0-7` | 从 `(259.0, 558.0)` 到 `(590.4, 558.0)`，间距约 `47.3` |
| 技能图标 | `Skill0-7` | 从 `(279.8, 553.1)` 到 `(610.9, 553.1)`，间距约 `47` |
| 禁招/冷却覆盖 | `jinzhao0-4` | 从 `(256.1, 556.5)` 开始，覆盖在技能框上 |

可迁移规则：

- Wheatear 可采用“左道具、中技能、右系统”的底部 HUD。
- 主动技能需要图标、冷却灰罩、倒计时数字和可用高亮。
- `J` 普攻不一定占冷却槽；`S+J` 上挑、`U/I/L` 这类主动技能和支援技能应占技能槽。
- `K` 回归一段跳后，HUD 上应显示为移动能力提示，而不是攻击技能图标。

### 2.3 右下系统按钮

右侧系统按钮沿底部排布：

| 元件 | 坐标 | 可对应 Wheatear |
| --- | --- | --- |
| `jijiaanniu` | `(649.5, 544.2)` | 装备 / 角色 |
| `qiannenganniu` | `(715.4, 542.0)` | 技能树 / 魔剑 |
| `chongwuanniu` | `(769.5, 544.1)` | 支援 / 队友 |
| `beibaoanniu` | `(830.0, 544.5)` | 背包 |
| `xitonganniu` | `(901.1, 544.1)` | 系统菜单 |

可迁移规则：

- 据点和战斗中的通用入口可以保持图标位置一致，减少玩家记忆成本。
- 图标按钮要比文字按钮优先，文字只在悬浮 tooltip 或详情面板出现。

### 2.4 Buff 行

Buff 图标位于底部技能栏上方：

| 元件 | 坐标 |
| --- | --- |
| `buff1-8` | 从 `(5.3, 499.4)` 到 `(213.6, 499.4)` |

可迁移规则：

- Wheatear 可把队友支援 Buff、白魔法 Buff、黑魔法 Debuff 状态放在技能栏上方。
- 不要把 Buff 放到屏幕中央，避免和浮空连击、Boss 受击反馈抢视觉中心。

### 2.5 Boss 血条

Boss 血条资源显示为居中偏上的长条：

| 元件 | 坐标 | 作用 |
| --- | --- | --- |
| `bosstouxiang` | `(285.0, 62.0)` | Boss 头像 |
| `bossziti1-4` | `(362.9, 74.2)` 到 `(568.6, 74.2)` | Boss 名字和等级文本 |
| `bossxuetiao1` | `(362.5, 102.0)` | Boss 血条 |
| `chengji` | `(723.0, 106.3)` | 评价 / 计数 |

可迁移规则：

- Wheatear Boss 血条应该放在顶部中间，与玩家状态分开。
- Boss 受击保护槽可以放在 Boss 血条下方，用较细的第二条显示。
- 断限可用窗口出现时，Boss 保护槽和目标身上特效同时闪烁。

## 3. 背包与装备页

参考资源：

| 文件 | 导出类 | 舞台 |
| --- | --- | --- |
| `j_bagpanelv20.swf` | `Bag_panel` | `960x600` |
| `j_wareroomPanelv39.swf` | `Room` | `960x600` |

### 3.1 背包页

背包页有顶部分类、左侧格子、底部货币和右侧提示区域。

顶部分类页签：

| 元件 | 坐标 |
| --- | --- |
| `bg_0` | `(12.4, 0.0)` |
| `bg_1` | `(85.8, 0.0)` |
| `bg_3` | `(159.1, 0.0)` |
| `bg_2` | `(232.4, 0.0)` |

物品格是 `4x5`：

| 行 | y | x 坐标 |
| --- | --- | --- |
| 1 | `48.1` | `22.1, 92.5, 162.9, 233.3` |
| 2 | `116.0` | `22.1, 92.5, 162.9, 233.3` |
| 3 | `184.0` | `22.1, 92.5, 162.9, 233.3` |
| 4 | `251.9` | `22.1, 92.5, 162.9, 233.3` |
| 5 | `319.9` | `22.1, 92.5, 162.9, 233.3` |

底部货币：

| 元件 | 坐标 |
| --- | --- |
| `jingbi_0` | `(2.8, 417.6)` |
| `gold_tx` | `(47.2, 418.1)` |
| `xingzhuang_1` | `(160.2, 417.6)` |
| `dj_tx` | `(201.2, 418.2)` |

提示 / 详情相关：

| 元件 | 坐标 | 说明 |
| --- | --- | --- |
| `btnTs` | `(507.4, 60.9)` | 提示按钮 / 帮助 |
| `mouse_mc` | `(495.6, 67.3)` | 鼠标跟随提示框 |
| `t_0` | `(304.8, 246.2)` | 右侧或下方文本块 |
| `t_1` | `(304.8, 292.8)` | 右侧或下方文本块 |
| `t_2` | `(304.1, 338.5)` | 右侧或下方文本块 |

可迁移规则：

- Wheatear 装备/背包页应保留“分类页签 + 格子区 + 详情操作区 + 货币资源区”。
- 背包格只显示图标、品质边框、数量和强化等级，不塞长文。
- 悬浮显示轻量 tooltip，点击后右侧详情面板显示装备、脱下、强化、来源。
- 背包格必须连续填充，装备后从背包列表消失，脱下后回到背包列表。

### 3.2 仓库页

仓库页使用更大的 `6x5` 格子：

| 行 | y | x 坐标 |
| --- | --- | --- |
| 1 | `141.1` | `119.3, 187.6, 255.8, 323.9, 392.1, 460.4` |
| 2 | `209.1` | `119.3, 187.6, 255.8, 323.9, 392.1, 460.4` |
| 3 | `277.0` | `119.3, 187.6, 255.8, 323.9, 392.1, 460.4` |
| 4 | `344.9` | `119.3, 187.6, 255.8, 323.9, 392.1, 460.4` |
| 5 | `412.9` | `119.3, 187.6, 255.8, 323.9, 392.1, 460.4` |

顶部分类：

| 元件 | 坐标 |
| --- | --- |
| `c_0` | `(87.5, 87.2)` |
| `c_1` | `(171.7, 86.3)` |
| `c_2` | `(255.9, 86.5)` |
| `c_3` | `(340.1, 86.5)` |
| `c_4` | `(424.3, 81.5)` |

底部操作：

| 元件 | 坐标 |
| --- | --- |
| `zl_0` | `(181.2, 523.8)` |
| `out_0` | `(358.9, 523.8)` |

可迁移规则：

- `PagedInventoryGrid` 应支持列数、行数、起点、格距配置。
- 背包页可以用 `4x5`，仓库 / 图鉴 / 信件附件页可以用 `6x5`。
- 格子控件应和分页控件解耦：格子只负责显示数据，分页器负责切页。

## 4. 角色属性页

参考资源：

| 文件 | 导出类 | 舞台 |
| --- | --- | --- |
| `j_playerpanelv34.swf` | `Player_panel` | `960x600` |

角色页是典型双列属性表：

| 区域 | 坐标规律 |
| --- | --- |
| 左侧属性行 | `d_0` `(89.8,139.2)`、`d_1` `(88.8,198.8)`、`d_2` `(88.8,259.8)`、`d_3` `(88.8,319.9)` |
| 右侧属性行 | `d_4` `(485.7,139.2)`、`d_5` `(485.7,198.8)`、`d_6` `(485.7,259.8)`、`d_7` `(485.7,320.9)` |
| 大背景行 | 左列约 `(63.2,179.1)` 起，右列约 `(461.4,179.1)` 起 |
| 底部职业 / 类型 | `type_mc` `(215.5,449.2)` |
| 底部经验 | `exp_mc` `(238.3,557.7)` |
| 关闭按钮 | `close_btn` `(897.6,9.5)` |

可迁移规则：

- Wheatear 角色面板可采用“双列属性 + 底部经验”的布局。
- 基础属性、装备加成、队友支援加成要分色显示。
- 属性说明用悬浮 tooltip，不把说明文字直接塞进属性表。

## 5. 技能面板

参考资源：

| 文件 | 导出类 | 舞台 |
| --- | --- | --- |
| `j_zhiyejinengv29.swf` | `zhiyejineng` | `960x600` |

结构观察：

| 区域 | 元件 | 坐标 |
| --- | --- | --- |
| 顶部页签组 | `jnannjiu` | `(63.1,55.1)` |
| 职业技能页签 | `zhiyejineng` | `(55.6,47.5)` |
| 五行技能页签 | `wuxingjineng` | `(194.6,47.5)` |
| 主技能列表容器 | `zhiyeheji` | `(45.0,127.8)` |
| 五行技能列表容器 | `wuxingheji` | `(45.0,127.8)` |
| 右侧 NPC / 角色预览 | `jnnpc` | `(690.8,183.1)` |
| 描述区域 | `jinengmiaoshu` | `(-183.3,396.4)` |
| 学习按钮 | `jnxuexi` | 约 `(600.7,513.7)` |

职业技能列表是纵向条目：

| 元件 | 坐标 |
| --- | --- |
| `zhiyetubiao1` | `(49.1,123.1)` |
| `zhiyetubiao2` | `(49.1,185.9)` |
| `zhiyetubiao4` | 约 `(49.1,311.7)` |
| `zhiyetubiao5` | 约 `(49.1,374.6)` |
| `zhiyetubiao6` | 约 `(49.1,437.6)` |
| `zhiyetubiao7` | 约 `(49.1,500.3)` |

可迁移规则：

- Wheatear 的魔剑大技能树仍应走“可拖动的大画布 + 节点网络”路线，不必改回竖列表。
- 但支援技能页、主动技能配置页、技能详情页可以参考这种“左侧条目、右侧预览、底部说明和学习按钮”的布局。
- 学习/升级按钮必须靠近材料需求和效果说明，避免玩家视线来回跳。

## 6. 强化页

参考资源：

| 文件 | 导出类 | 舞台 |
| --- | --- | --- |
| `j_strengpanelv22.swf` | `Streng_Panel` | `960x600` |

强化页结构非常适合 Wheatear 装备强化：

| 区域 | 元件 | 坐标规律 |
| --- | --- | --- |
| 页签 | `st_0`、`st_1` | `(118.2,82.7)`、`(255.7,82.7)` |
| 材料 / 装备槽 | `ed_0..ed_6` | 从 `(125.0,133.3)` 到 `(441.9,133.3)` |
| 当前装备图标 | `wp_0` / `w_0` | `(177.7,239.7)` / `(188.9,258.2)` |
| 目标装备图标 | `wp_1` / `w_1` | `(181.7,350.9)` / `(192.2,370.6)` |
| 名称文本 | `name_text` | `(362.2,232.2)` |
| 类型文本 | `type_text` | `(364.1,253.2)` |
| 当前属性 | `current_text` | `(364.1,274.6)` |
| 下级属性 | `next_text` | `(364.1,296.0)` |
| 金币消耗 | `gold_text` | `(355.2,364.8)` |
| 成功率 / 概率 | `pro_text` | `(355.2,390.8)` |
| 数量选择 | `num_mc` / `num_text` | `(166.8,432.2)` / `(222.2,435.3)` |
| 确认按钮 | `ok_0` | `(269.4,475.5)` |

可迁移规则：

- Wheatear 强化页应把“当前装备 -> 强化后预览”做成明确的上下或左右对比。
- 材料槽、金币消耗、成功率 / 保底信息要和确认按钮在同一视觉区域。
- 强化前必须显示属性变化，不让玩家只看到按钮。

## 7. 商店页

参考资源：

| 文件 | 导出类 | 舞台 |
| --- | --- | --- |
| `j_shoppanelv33.swf` | `Shop_Panel` | `960x600` |

商店页是 `3x4` 大卡片商品网格：

| 行 | y | x 坐标 |
| --- | --- | --- |
| 1 | `68.7` | `300.6, 508.6, 716.5` |
| 2 | `195.5` | `300.6, 508.6, 716.5` |
| 3 | `321.3` | `300.6, 508.6, 716.5` |
| 4 | `452.4` | `300.9, 508.9, 717.0` |

商品价格 / 按钮大致跟随商品卡：

| 元件 | 坐标规律 |
| --- | --- |
| `jt_0..jt_8` | 与前三行商品对应，x 约 `353.7, 563.6, 772.6` |
| `zz_0..zz_8` | 与前三行商品对应，x 约 `304.1, 512.0, 720.0` |
| `dt_0..dt_2` | 第四行按钮，x 约 `353.7, 563.6, 772.6` |

可迁移规则：

- 已知数量的商店商品适合分页卡片，而不是滚动长列表。
- 每个商品卡应显示图标、价格、限购、购买按钮。
- 商品详情可在悬浮 tooltip 或右侧详情区显示。

## 8. 对 Wheatear UI 的直接建议

### 8.1 战斗 HUD

- 采用 `960x600` 参考布局，按 Canvas 自适应缩放到目标分辨率。
- 左上：生命、魔剑能量、断限槽。
- 顶中：Boss 血条、Boss 保护槽。
- 底左：消耗品 / 临时道具槽。
- 底中：主动技能槽，必须支持图标、冷却灰罩、倒计时数字、按键标签。
- 底右：装备、技能树、支援、背包、系统图标按钮。
- 技能栏上方：Buff / Debuff 小图标。

### 8.2 据点与管理页面

- 背包、仓库、图鉴、邮件附件统一走 `PagedInventoryGrid`。
- 背包页默认 `4x5`，仓库 / 图鉴默认 `6x5`，商店默认 `3x4` 商品卡。
- 页签在上方，主要内容在左/中，详情和操作在右，下方放货币与确认按钮。
- 悬浮 tooltip 负责短说明；点击详情面板负责完整说明和操作。

### 8.3 编辑器组件需求

为了让这些布局能在 WheatearEditor 中复用，后续 UI 组件最好继续补齐：

| 组件 | 需要能力 |
| --- | --- |
| `UIPagedGridComponent` | 行列数、格子间距、起点、分页、连续填充 |
| `UISkillSlotComponent` | 图标、冷却灰罩、倒计时、按键标签、禁用覆盖 |
| `UIBuffRowComponent` | 横向小图标列表、层数、剩余时间、tooltip |
| `UITooltipComponent` | 悬浮跟随、边界避让、延迟出现 |
| `UITabGroupComponent` | 页签互斥、高亮、禁用、切页命令 |
| `UIComparePanelComponent` | 当前属性、升级后属性、变化值高亮 |

## 9. 当前结论

机甲小子的 UI 不是靠大量说明文字堆出来的，而是靠固定信息区域、图标按钮、格子网格、页签和短提示组织出来的。

Wheatear 后续应模仿这种结构感：

- 重要操作优先图标化。
- 说明文字只在需要时出现。
- 背包、仓库、商店、技能配置都用统一可复用控件。
- 战斗 HUD 保持底部一条操作带，避免遮挡战斗主体。
- 坐标和布局不要硬编码在游戏逻辑里，而是暴露到 Canvas / UI 控件参数，允许编辑器微调。
