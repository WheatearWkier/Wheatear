# 引擎工程化与编辑器升级记录

合并类型化引擎路线、场景编辑器升级记录和 2026-08-02 打磨记录。这里保留历史决策和演进脉络，日常查用优先看具体架构文档。

> 2026-08-15 更新：C#/Mono 脚本栈已整体从项目中移除（`Wheatear-ScriptCore`、
> `ScriptEngine/ScriptGlue`、vendor Mono 运行时，备份在 `backup/MonoAndScriptCore/`）。
> 本文中关于 C#/Mono 的段落均为**历史记录**，仅作演进脉络参考，不代表当前状态。

---

## 来源：类型化引擎路线与项目体检.md

## 类型化引擎路线与项目体检

更新日期：2026-07-31

### 1. 引擎方向

Wheatear 后续不追求做成“大而全但每个方向都很浅”的通用引擎，而是做成偏向特定 2D 游戏类型的生产型引擎。

当前内置玩法方向：

- 弹幕 / 假玩法模块：适合短流程、剧情反差、小游戏关卡。
- 回合制战斗：适合过渡章节、策略选择、角色技能表现。
- 2D 横版格斗：当前主玩法方向，重点是空连、滞空、断限追击、技能树和装备成长。
- 战棋战斗：适合假玩法、章节变体，后续也可扩展成独立战棋项目。
- VN / Galgame：承担剧情、角色关系、好感度、选择、存读档和 BGM 演出。

这些玩法模块应当复用同一套引擎公共层，而不是各自重复造轮子。

### 2. 公共层拆分原则

多个玩法都会用到的能力，应放到公共层：

- 场景与实体：`Scene`、`Entity`、`UUID`、`EntityReference`。
- 资源与打包：`AssetPath`、`AssetDependencyScanner`、`.wtpack`、打包报告。
- UI：Canvas、Widget、Panel、Pager、ScrollView、SkillTreeView、Path。
- 动画：SpriteSheet、AnimationClip、动画事件、`anim:` 命令。
- 音频：BGM、SFX、音量设置、AudioSource。
- 流程：`CommandBus`、`.wts`、`SceneTransitionService`。
- 成长数据：经验、材料、装备、技能、好感度、支援。

玩法模块只负责自己的规则、状态机、AI、关卡逻辑和专属表现。例如 SideCombat 不应该自己实现一套资源依赖扫描，战棋也不应该自己实现一套分页 UI。

### 3. UUID 引用规范

Wheatear 使用 UUID 作为稳定实体引用。编辑器中不要求用户手填 UUID，用户看到的应该是对象槽、实体名、选择按钮和清除按钮；底层保存 UUID。

推荐数据形态：

```yaml
TargetEntity: 17382910492300000000
```

规则：

- 优先通过 `TargetEntity` / UUID 解析。
- 名字和 Tag 只用于显示、搜索、编辑器检索和人类阅读，不再作为运行时主引用。
- 运行时目标引用必须保存 UUID；旧的名字 / Tag 目标写法不再作为兼容路径保留。
- 跨场景引用未来需要同时保存场景路径或场景资源 GUID。

当前命令系统的目标选择规则：

```text
event:open_skill_tree
event:@17382910492300000000:open_skill_tree
anim:play:@17382910492300000000:idle
ui:pager:@17382910492300000000:next
```

`event:<name>` 表示广播给当前场景所有 `EventScriptComponent`；如果需要指定实体，使用 `event:@<UUID>:<name>`。`anim:` 和 `ui:pager:` 这类必须锁定具体目标的命令统一使用 `@UUID`。

### 3.1 玩法公共服务层首批落地

四类玩法的第一批公共能力已经抽到 `Wheatear/Modules/Common`：

- `GameplayAudioService`：统一 BGM / SFX 音量、主音量和播放入口，玩法模块不再各自计算音量。
- `GameplayCombatService`：统一基础伤害、防御、治疗、死亡判断，避免假玩法、横板、回合制、战棋各写一套公式。
- `GameplayEntityService`：统一通过 UUID 解析运行时实体，配合上面的引用规范。
- `GameplayFlowService`：统一处理胜利、失败、结算等延迟命令派发。

当前已经接入这些服务的玩法：弹幕假玩法、2D 横版格斗、回合制战斗、战棋战斗。

第二批已经补上的公共服务：

- `GameplayTextService`：命令分割、帧序列路径格式化、数值格式化。以后不要在各玩法里重复写 `SplitCommand` / `{frame}` 替换。
- `GameplayVisualService`：共享贴图缓存、Sprite / UI 序列帧应用。以后临时单帧和 spritesheet 迁移前的帧条都先走这里。
- `GameplayTargetingService`：按组件收集实体、队伍存活判断、最近/最低血目标选择。回合制、战棋、横板 AI 都能复用。
- `GameplayUIService`：血条、资源条、tooltip 等 HUD 小原语。玩法模块只给实体名和数值，不再重复写显示细节。
- `GameplayUILayoutService`：运行时创建/同步 Pager、ScrollView、Text、Button、Slider、PageItem 等页面组件。Progression 的装备页、存档页、设置页已开始复用。
- `GameplayRewardService`：奖励摘要解析、掉落数量判断。结算页掉落图标不再把解析逻辑写死在 Progression 里。

后续继续拆的方向是：`SideCombatTuningService`、`SideCombatActionService`、`CombatResultService`、`AnimationEventBridgeService`、以及 Progression 下的 `Inventory / Equipment / SkillTree / Relationship / SaveGame` 子服务。

### 4. Project Health

`View / Project Health` 是项目体检面板，用于交付前检查：

- 从启动场景递归扫描资源依赖。
- 解析 `.wt`、`.wtprefab`、`.wtmaterial`、`.vn`、`.wts`、`.yaml`、`.yml`、`.json`。
- 列出会被打包的资源。
- 列出缺失引用。
- 列出未被启动流程引用的资源。
- 显示已解析文本资产和扫描警告。

这解决的问题是：编辑器、打包器、运行版使用同一套依赖判断，避免“编辑器能看到，打包版缺资源”的情况。

### 5. 打包报告

`Package Player` 成功后，输出目录会生成：

```text
package_report.txt
```

报告包含：

- 启动场景。
- 是否启用 C# 脚本。
- 打包资源数量。
- 源资源大小。
- `.wtpack` 大小。
- 缺失引用。
- 实际写入 `.wtpack` 的资源列表。

以后每次准备发给别人试玩前，应先看 `Project Health`，再打包并检查 `package_report.txt`。

### 6. 下一步工程化目标

短期优先：

- Inspector 对象槽：把常见实体名字字段升级成可选择实体的 UUID 引用槽。
- Command 自动补全：根据场景实体、事件名、资源路径提示命令。
- SideCombat 拆分：输入/手感、动作状态机、命中解析、AI、HUD、掉落结算、调参加载分文件。
- Progression 拆分：Inventory、Equipment、SkillTree、Relationship、SaveGame 分模块。
- Project Health 扩展：增加脚本事件不存在、场景跳转不存在、重复 Tag、孤儿 UI、未挂 Canvas UI 的检查。

长期目标：

- 所有类型玩法共享 UI、动画、资源、存档、命令、音频、调参工具。
- 每种玩法只保留自己的核心规则和编辑器内容面板。
- 编辑器尽量让策划通过面板完成内容制作，减少手写 YAML 和手改场景文件。

### 7. 2026-07-31 模块服务层落地规则

本轮开始把“玩法系统大文件”拆成稳定层级：

- `System`：只做运行时入口、每帧调度、场景生命周期桥接。
- `TuningService`：加载 YAML/数据表、默认参数、热重载缓存、章节解锁 profile 查询。
- `ResultService`：评分、经验、奖励结算、结算摘要等可独立测试的公式。
- `PageService`：某个具体运行时页面的 UI 组装、分页、tooltip、拖动画布、局部刷新。
- `Common Gameplay Service`：多个玩法共用的文字、目标选择、音频、奖励、通用 UI、运行时布局等小原语。

当前已落地示例：

- 横板战斗：`SideCombatTuningService`、`SideCombatResultService`。
- 成长/据点页面：`ProgressionEquipmentPageService`、`ProgressionResultPageService`、`ProgressionSaveLoadPageService`、`ProgressionSettingsPageService`、`ProgressionSkillTreePageService`。

后续新增弹幕、回合制、横板格斗、战棋功能时，先判断逻辑属于规则、数据、页面、结果、流程还是公共原语，再放入对应层级。不要再把目标选择、奖励掉落、HUD 构建、调参加载、存档页刷新直接堆到玩法主系统文件里。
### 8. 2026-07-31 玩法系统继续服务化原则

Wheatear 后续的玩法模块按“玩法是状态机，能力是服务”的原则继续拆分。玩法系统本体只保留本玩法独有的循环、阶段切换、输入入口和调度顺序；凡是可以被多个玩法复用，或者可以独立测试、独立配置、独立编辑的逻辑，都优先放入服务层。

#### System 层

System 层回答“当前玩法这一帧处于什么阶段、应该调用哪些服务”。例如横板战斗的起手、浮空、断限、结算阶段；回合制的等待指令、选择目标、行动演出、胜负判断；战棋的选单位、移动、技能目标、敌方回合。System 不再承载大块技能库、掉落计算、UI 页面构建、投射物细节、棋盘寻格等通用能力。

#### Service 层

Service 层回答“具体能力怎么做”。同一种能力可以服务于多种玩法，例如：

- SkillService：技能定义、技能查找、槽位解析、技能消耗、伤害/治疗基础公式。
- TargetService：目标收集、阵营过滤、最低血量/最近距离/合法目标选择。
- BoardService：格子坐标、范围判断、占位查询、可移动格、简单 AI 步进。
- ProjectileService：投射物创建、贴图选择、生命周期、碰撞、遮挡。
- ResultService：胜负、评级、奖励、掉落、结算页面数据。
- UIService：血条、资源条、tooltip、分页、滚动、技能栏表现。

#### 数据和脚本

服务层不应该把未来内容锁死在 C++ 中。当前竖切可以先保留内置默认库，保证 demo 可运行；但服务 API 要向数据表友好，后续可以把技能、敌人、掉落、AI 策略、章节条件迁移到 YAML/编辑器表，再由事件脚本串起流程。

#### 这轮拆分目标

本轮继续把四种玩法中最稳定的公共能力拆出去：

- 回合制：技能库与目标选择从 `TurnCombatSystem` 拆到 `TurnCombatSkillService` / `TurnCombatTargetService`。
- 战棋：技能库与棋盘格逻辑从 `TacticalCombatSystem` 拆到 `TacticalCombatSkillService` / `TacticalCombatBoardService`。
- 假弹幕玩法：投射物创建、清理、碰撞从 `ArcadeCombatSystem` 拆到 `ArcadeCombatProjectileService`。
- VN：暂时保持剧情状态机主体不动，后续优先把设置页、存档页、历史记录页拆成独立 presentation/service。

最终目标是：以后新增玩法时，只新增“状态机和少量规则胶水”，而不是复制一整套战斗、UI、掉落、目标选择和数据处理代码。

### 9. 2026-07-31 横板战斗服务层第二批拆分

横板战斗继续按“System 只负责循环，Service 负责能力”的方式拆分。当前 `SideCombatSystem` 保留玩家输入、敌人 AI 阶段、命中结算规则、物理推进、死亡胜负和场景流程。下面能力已经拆出去：

- `SideCombatTuningService`：参数读取、默认值、技能解锁、Boss 保护条/断限 UI 显示条件。
- `SideCombatResultService`：结算评级、经验奖励、结果摘要。
- `SideCombatHudService`：战斗 HUD、技能栏、冷却遮罩、tooltip、道具栏、血条/连击/奖励文字。
- `SideCombatActionService`：玩家/敌人动作运行态、动作持续时间、取消窗口、动作清理、挥动音效入口。
- `SideCombatHitboxService`：hitbox 创建、归属销毁、帧贴图更新、空间重叠检测。

这次没有把完整命中结算直接搬出 `SideCombatSystem`，因为它仍然和浮空、断限、Boss 保护条、连击计数、魔剑槽、受击状态、敌我动作打断紧密耦合。下一步更合理的拆法是先定义一个 `SideCombatHitResult` 数据结构，再把命中结算拆成：

- `SideCombatHitResolutionService`：伤害、硬直、击飞、受击状态、保护条增长/破防。
- `SideCombatComboService`：连击计数、空中命中奖励、魔剑槽增长、断限条件结果。
- `SideCombatPickupService`：掉落物创建、吸附、拾取入库。

这样拆分后，`SideCombatSystem` 会进一步收缩成横板动作状态机：输入采样、玩家状态推进、敌人状态推进、物理推进、调用命中/表现/奖励服务。
### 10. 2026-07-31 横板命中与连击服务拆分

本轮把横板战斗的命中结算继续从 `SideCombatSystem` 拆出，保持“玩法系统是状态机，服务层负责能力”的原则。

新增服务：

- `SideCombatHitResolutionService`：负责命中后的扣血、受击硬直、击飞、死亡状态、玩家受击计数、敌我动作打断、Boss 保护条增长与破防。
- `SideCombatComboService`：负责连击计数、断连计时、玩家空中命中奖励、魔剑槽增长。

当前边界：

- `SideCombatSystem` 仍然负责输入采样、玩家行动选择、敌人 AI 阶段、hitbox 扫描循环、物理推进、死亡/胜负流程。
- `SideCombatHitboxService` 负责“是否发生空间命中”和 hitbox 生命周期。
- `SideCombatHitResolutionService` 负责“命中后目标变成什么状态”。
- `SideCombatComboService` 负责“玩家因为命中获得什么连招收益”。

这样拆完后，横板战斗的核心命中段不再直接散落大量扣血、击飞、保护条、连击和魔剑槽代码。下一步如果继续优化，应优先拆：

- `SideCombatPickupService`：掉落物创建、吸附、拾取入库。
- `SideCombatPhysicsService`：横板纵深移动、空中高度、重力、落地、Boss 保护恢复。
- `SideCombatEnemyAIService`：敌人接近、攻击选择、Boss 阶段行为。

### 11. 2026-07-31 横板战斗完整服务层收束

这轮把 `SideCombatSystem` 从“玩法大文件”收束成调度层。主系统现在只负责：

- 采样输入。
- 找到当前关卡、玩家、Boss。
- 处理暂停和 hit pause 的时间缩放。
- 按顺序调度横板服务。
- 刷新 HUD。

横板战斗服务边界如下：

- `SideCombatLifecycleService`：关卡运行时状态重置、角色运行时字段初始化、玩家属性和 Boss 参数应用。
- `SideCombatPlayerService`：玩家输入解释、移动、跳跃缓冲、技能起手、空中动作次数、断限追击起手。
- `SideCombatEnemyAIService`：敌人醒来、接近玩家、横向/纵深走位、Boss 血量阶段技能选择、敌人攻击动作推进。
- `SideCombatHitboxService`：hitbox 创建、贴图帧更新、生命周期、空间重叠检测、命中扫描。
- `SideCombatHitResolutionService`：命中后的伤害、受击状态、击飞、死亡、玩家受击计数、Boss 保护条。
- `SideCombatComboService`：连击计数、断连计时、空中命中奖励、魔剑槽增长。
- `SideCombatPhysicsService`：横板纵深移动、空中高度、重力、落地、保护恢复、角色视觉同步入口。
- `SideCombatPickupService`：掉落创建、吸附、拾取入库。
- `SideCombatOutcomeService`：敌人死亡处理、胜利判定、结算记录、胜负后场景跳转。
- `SideCombatFeedbackService`：音效、hit pause、镜头震动、开场/结算淡入淡出。
- `SideCombatVisualService`：角色动画 clip 选择、SpriteAnimator 接入、受击/霸体/死亡颜色反馈、影子同步。
- `SideCombatTargetService`：横板玩法内目标选择，例如最近存活敌人。

这就是后续类型化玩法的参考结构：`System` 是状态机和调度器，`Service` 是可测试、可复用、可编辑器化的能力单元。以后新增弹幕、回合制、战棋、横板新章节时，不应该再把目标选择、奖励、UI、物理、动画、命中、AI 直接堆回 System。

### 12. 2026-07-31 三种玩法主系统大清洗

本轮把类元气假玩法、回合制战斗、战棋战斗也按同一架构原则完成服务化：`System` 只保留玩法循环、输入入口、命令分发和服务调度；具体能力拆到模块服务。这样以后新增同类型玩法时，可以直接参考对应模块，而不是继续复制大文件。

公共层边界保持克制：
- `Modules/Common` 只放真正跨玩法稳定复用的小能力，例如音频播放、基础伤害/治疗公式、实体 UUID 解析、奖励解析、通用 UI 原语、文本命令工具、目标集合筛选。
- 浮空、断限、格子移动、回合队列、弹幕投射物节奏这类规则虽然都属于“战斗”，但数据结构和手感骨架不同，保留在各自玩法模块。

#### ArcadeCombat

`ArcadeCombatSystem.cpp` 从约 524 行收束到约 119 行。当前服务边界：
- `ArcadeCombatLifecycleService`：关卡运行时重置、战斗者重置、Boss 开场表现重置。
- `ArcadeCombatPresentationService`：开场淡入、触发圈闪烁、Boss 入场触发和入场动画推进。
- `ArcadeCombatPlayerService`：玩家移动、武器切换、攻击输入、朝向和开火。
- `ArcadeCombatBossService`：Boss 跳跃、追踪、阶段攻击节奏。
- `ArcadeCombatProjectileService`：投射物创建、生命周期、碰撞、命中和清理。
- `ArcadeCombatOutcomeService`：胜负后延迟、淡出和场景流转命令。
- `ArcadeCombatHudService`：血条、武器名、暂停提示、胜负提示。
- `ArcadeCombatMath`：俯视 2D 场景常用距离和方向计算。

#### TurnCombat

`TurnCombatSystem.cpp` 从约 712 行收束到约 38 行。当前服务边界：
- `TurnCombatSkillService`：技能定义、技能槽解析、伤害/治疗基础数值。
- `TurnCombatTargetService`：战斗者收集、回合队列、合法目标和 AI 目标选择。
- `TurnCombatVisualService`：站位视觉、受击闪烁、技能特效和动画帧。
- `TurnCombatActionService`：行动开始、技能应用、伤害/治疗/防御/充能结算。
- `TurnCombatCommandService`：`turn:` 命令解析和按钮操作落点。
- `TurnCombatUIService`：状态栏、指令栏、目标提示、按钮可用性。
- `TurnCombatFlowService`：战斗重置、下一回合、胜负、行动计时和每帧阶段推进。

#### TacticalCombat

`TacticalCombatSystem.cpp` 从约 717 行收束到约 38 行。当前服务边界：
- `TacticalCombatBoardService`：格子坐标、世界坐标换算、范围、占位、移动格和最近敌人。
- `TacticalCombatSkillService`：战棋技能定义、技能槽、范围、伤害/治疗和特效帧。
- `TacticalCombatFeedbackService`：战棋模块音效入口。
- `TacticalCombatVisualService`：单位视觉、血条颜色、选中标记、行动特效和隐藏。
- `TacticalCombatActionService`：行动开始、移动/攻击/治疗结算、行动结束。
- `TacticalCombatAIService`：敌方单位回合、靠近玩家、攻击选择和阶段切换。
- `TacticalCombatCommandService`：`tactic:` 命令解析、格子点击、技能点击和目标选择。
- `TacticalCombatUIService`：棋盘格显示、状态文字、技能按钮、行动详情。
- `TacticalCombatFlowService`：关卡重置、玩家/敌方回合阶段、胜负、淡入和每帧更新。

#### 后续约束

新增或修改玩法时先问三个问题：
- 这是跨玩法稳定原语吗？是的话放 `Modules/Common`。
- 这是某个玩法独有能力吗？是的话放 `Modules/<玩法>/<玩法>...Service`。
- 这是本玩法当前阶段怎么流转吗？是的话才留在 `*System`。

这条规则优先级高于“为了少建文件而临时写进 System”。System 文件如果重新膨胀到几百行，说明应该继续拆。

### 13. 2026-08-01 玩法层后续统一模板

当前玩法层已经够支撑竖切开发，但后续为了继续增加更多玩法，新增或重构玩法模块时统一按下面模板组织：

- `*System`：只做玩法循环、阶段切换、输入采样、服务调度、运行时生命周期。
- `*ActionCatalog`：把技能、武器、敌人招式、行动定义映射为 WAO `ActionRecipe`。
- `*ActionService`：处理动作运行态、动作开始/结束、命中帧、取消窗口、动作资源消耗。
- `*TargetService`：处理玩法内目标选择；如果规则稳定且跨玩法可复用，再下沉到 `Modules/Common`。
- `*ResultService`：处理胜负、评分、奖励、结算摘要。
- `*HudService` / `*UIService`：处理运行时 HUD 和页面刷新，不把 UI 细节堆回 System。
- `*VisualService` / `*FeedbackService`：处理动画、VFX、音效、hit pause、震屏等表现。
- `*TuningService`：处理 YAML/数据表加载、默认参数、热重载、章节 profile。
- `EditorPanel` / `Drawer`：处理该玩法的编辑器内容生产入口。

WAO 在这个模板中的位置：

- `ActionRecipe` 描述动作语义、时序、冷却、资源、效果预览和表现信号。
- `ActionDebugHistory` 记录运行时动作和效果账本，服务编辑器调试。
- `ActionSignalRouter` 负责把动作表现信号发给音频、VFX、UI、脚本等 handler。
- 玩法独有的命中、格子、浮空、投射物、回合队列暂时不强行公共化；未来通过 `ActionResolver` 做适配层。

当前最值得继续打磨的点：

1. 横板战斗：把真实命中后的伤害、浮空、保护条、魔剑槽、连击奖励写入 `EffectLedger`。
2. 弹幕玩法：把投射物创建从硬编码调用逐步迁成 `ActionSignalRouter` handler。
3. 回合制/战棋：把技能定义进一步映射为 `ActionRecipe`，让 WAO 面板也能看这些技能。
4. 编辑器：给 WAO Action Debugger 增加“按玩法模块分组”和“从 recipe 跳转到调参表/资源”的能力。

### 14. 2026-08-01 工程接入自动校验

本轮新增工程源文件同步检查，解决 C++ 大工程里常见的一个问题：新增 `.cpp/.h/.inl` 后忘记加入 `.vcxproj`，导致本机某些构建方式能过、Visual Studio 或打包构建不过。

命令行入口：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\Check-ProjectSources.ps1
```

当前检查范围：

- `Wheatear/src` 对应 `Wheatear/Wheatear.vcxproj`
- `WheatearEditor/src` 对应 `WheatearEditor/WheatearEditor.vcxproj`
- `WheatearSandbox/src` 对应 `WheatearSandbox/WheatearSandbox.vcxproj`

编辑器入口：

```text
View -> Project Health -> Source Sync
```

它会列出两类问题：

- `Missing From Project`：磁盘上有源码，但工程文件没有登记。
- `Stale Project Entry`：工程文件登记了源码，但磁盘上没有这个文件。

这属于“工业化护栏”，不是玩法功能。以后每次新增服务、面板、玩法模块，都应先跑一次这个检查，再跑构建。

### 15. 2026-08-01 数据资产进入打包依赖

`AssetDependencyScanner` 已把下面目录作为引擎运行所需的内置数据资产加入打包依赖：

```text
assets/gameplay/actions
```

因此 WAO action YAML 不需要被某个具体场景逐个显式引用，也会进入运行包。这样技能/武器/玩法 action 数据可以独立于场景存在，后续也更适合编辑器化和批量维护。
### 16. 2026-08-01 运行时偏好与打包门禁

本轮把两类“交付前必须稳定”的能力继续工程化：

#### 运行时偏好

`Window` 接口新增 `SetFullscreen` / `IsFullscreen`，Windows 平台实现基于 GLFW monitor/windowed 切换，并保留窗口模式下的位置与尺寸。`GameProgress::ApplySettingsToRuntime()` 会把存档里的全屏偏好应用到当前窗口；新游戏、读档和设置页命令都会走这一入口。

音量默认值下调到更适合试玩的区间，仍然通过 `AudioEngine::PercentToGain()` 的非线性曲线进入 `GameplayAudioService` 和 `AudioSystem`。震屏开关继续由 `GameProgress::Settings.ScreenShake` 统一控制，横板战斗反馈服务每次触发震屏前都会读取该设置。

#### 打包门禁

`AssetDependencyScanner` 现在会额外提取这些流程命令中的场景目标：

```text
scene:assets/scenes/xxx.wt
newgame:assets/scenes/xxx.wt
loadgame:assets/scenes/xxx.wt:1
```

扫描结果会分成：

- `SceneTransitions`：当前依赖图里发现的所有场景跳转。
- `MissingSceneTransitions`：跳转目标不存在或断链的场景。
- `MissingReferences`：普通资源引用缺失。

`Project Health` 新增 `Scene Links` 页签，打包报告新增 `Scene Transition List` 和 `Missing Scene Transitions` 小节。`Package Player` 在写入 `content.wtpack` 前会执行 preflight：如果存在缺失资源或缺失场景跳转，会生成 `package_preflight_report.txt` 并停止打包，避免产出一个运行时才断流程的包。

验证记录：Debug x64 全工程构建通过；Editor/Sandbox 6 秒烟测通过；CLI 无脚本打包通过；打包版 `WheatearSandbox.exe` 6 秒烟测通过。

---

## 来源：场景编辑器升级记录.md

## 场景编辑器升级记录

更新时间：2026-07-29

本文记录 WheatearEditor 的本轮工业化升级方向和已落地内容。目标是让编辑器逐步从“能加载场景”升级为“能直接手调场景和 UI 的生产工具”。

### 本轮目标

- 解决旧版 Dear ImGui 接口过旧、控件能力不足、API 与源码混杂的问题。
- 让 UIWidgetComponent 可以在 Scene Viewport 中直接选中、移动和缩放。
- 改善 Scene Hierarchy 和 Inspector 的查找、分类、锚点、层级、布局预设等编辑体验。
- 为后续撤销重做、多选、对齐吸附、Prefab、UI 蓝图化打基础。

### 第三方库升级

- Dear ImGui：升级到官方 docking 分支 1.92.9。
- ImGuizmo：同步官方 master 新版源码。
- ImGui OpenGL/GLFW backend：从旧 examples 路径迁移到新版 backends 路径。
- ImGui 静态库构建：补入新版核心文件 imgui_tables.cpp。

升级原因：

- 旧版 imgui.h 标记为 1.79 WIP，但 imgui.cpp 中混有 docking/tables 代码，源码状态不干净。
- 旧版 API 缺少新版表格、Docking、输入事件和纹理接口能力。
- 新版 ImGui 明确移除了 GetKeyIndex、旧 ImageButton 纹理接口等旧写法，需要项目侧同步适配。

### 已落地编辑器能力

#### 编辑器命令与撤销复原

- 新增通用 `CommandHistory`，支持 `Ctrl+Z` 撤销、`Ctrl+Y` / `Ctrl+Shift+Z` 复原。
- 创建、删除、复制实体进入撤销栈。
- Add Component、Remove Component、Reset Component 进入撤销栈。
- Inspector 常用字段修改进入撤销栈。
- Viewport Gizmo 移动、旋转、缩放结束后生成撤销记录。
- UI 控件拖动和缩放进入撤销栈。
- 文本 UI 在拖拽缩放时同步调整字号，并和 UI 尺寸变化合并为同一条撤销记录。

#### 编辑器布局持久化

- ImGui 布局配置从运行目录迁移到 `%LOCALAPPDATA%/Wheatear/Wheatear Editor/imgui.ini`。
- 重新构建并覆盖 `Builds/Windows/Editor/WheatearEditor.exe` 后，用户自定义 Docking 布局不会重置。
- 构建脚本复制编辑器输出时会排除旧的 `imgui.ini`，避免把临时布局带入打包目录。
- 首次启动且没有已有 `imgui.ini` 时，编辑器会自动铺默认 Docking 布局。
- `View / Reset Window Layout` 可以主动恢复默认窗口布局。

#### Viewport 相机归位

- Viewport 内双击左键会把编辑器相机对齐到场景 `Primary` Camera 的位置和朝向。
- 编辑器相机保持透视编辑投影，不继承运行时相机的正交投影，避免归位后继续拖动视窗时操作异常。
- 同步后 Scene Hierarchy 会选中对应 Camera 实体，使左侧高亮明确表示当前对齐的是哪台相机。
- `View / Focus Primary Camera` 提供同样的菜单操作。

#### Scene Viewport UI 编辑

- 选中 UIWidgetComponent 后，Viewport 内显示 UI 边框。
- 可显示全部 UI 控件轮廓，方便检查层级和遮挡。
- 可点击 Viewport 内 UI 控件进行选中，按 SortOrder 从上到下命中。
- 可拖动 UI 控件主体移动位置。
- 可拖动八方向 handle 缩放 UI 控件。
- 支持 ParentEntity(UUID) 父控件坐标换算，子 UI 在父区域内按局部坐标编辑。
- 选中有 ParentEntity 的控件时显示父区域边界。
- Ctrl 拖拽时按细网格吸附，方便做像素级对齐。

#### Scene Hierarchy

- 增加搜索框，能按实体名过滤。
- 增加 UI-only 过滤，适合专门调 UI 场景。
- 实体名前增加类型前缀：[UI]、[Camera]、[3D]、[2D]、[Entity]。
- 创建 UI 菜单新增 Canvas。
- 除 Canvas 外，其他 UI 控件只能在选中 Canvas 或 Canvas 子控件时创建，避免产生孤儿 UI。
- 创建 UI 控件时设置默认 SortOrder，减少新建后遮挡顺序混乱。

#### Inspector UIWidget 编辑

- UIWidgetComponent 位置和尺寸改为更宽松的归一化编辑范围。
- 增加锚点九宫格，减少手填 Anchor 的成本。
- 增加常用布局预设：全屏、顶部栏、底部栏、左侧栏、右侧栏、居中面板。
- 增加微调按钮，方便小步移动。
- 增加 Bring Front / Send Back，快速调整 UI 层级。
- ParentEntity 对象槽支持选择和清空。

#### UI Canvas 专属编辑视窗

- 选中 `UICanvasComponent` 后，Inspector 中提供 `Edit Canvas` 按钮。
- 新增 `UI Canvas Editor` 窗口，只显示 UI 画布，不显示场景世界物体。
- Canvas Editor 支持点击选择 UI、拖动移动、拖 handle 缩放。
- Canvas Editor 只显示当前 Canvas 下的子控件；选中某个子控件时，其所属 Canvas 会以高亮背景提示。
- `Use Selected Canvas` 支持从 Canvas 子控件反查所属 Canvas。
- 主 Viewport 可勾选 `Hide UI In Scene Viewport` 暂时隐藏 UI，方便编辑被 UI 挡住的场景物体。

#### UI 父子层级显示

- 创建 UI 控件时，如果当前选中 Canvas 或 UI 父控件，会自动写入 `ParentEntity`。
- Scene Hierarchy 会按 `ParentEntity` 把子 UI 缩进显示到父 UI 下方。
- Canvas 默认展开，方便直接查看页面结构。
- 删除 UI 父节点时，右键删除会把子 UI 一起放入撤销操作，避免遗留不可见孤儿控件。

#### 新版 ImGui API 适配

- GetKeyIndex 写法替换为 ImGui::IsKeyPressed(ImGuiKey_A)。
- ImageButton 全部适配新版显式 str_id + ImTextureRef 接口。
- ImTextureID 强转从旧 reinterpret_cast 改为整数句柄 static_cast。
- ImGuiViewport::GetWorkPos/GetWorkSize 改为 WorkPos/WorkSize 字段。
- 旧 ImGuiTreeNodeFlags_AllowItemOverlap 改为 ImGuiTreeNodeFlags_AllowOverlap。
- 旧 ImFont::FontSize 访问改为 ImGui::GetFrameHeight。

### 验证结果

- 已执行 Debug 全工程构建：通过。
- 已执行 WheatearEditor 隐藏窗口启动烟测：运行 8 秒未早退，手动停止。
- 构建仍存在 Mono/第三方库 PDB 缺失警告，属于既有调试符号警告，不影响本轮功能。

### 当前限制

- UI 编辑暂不支持多选、框选、对齐、等距分布。
- UI resize 当前按矩形编辑，未处理旋转后控件的真实旋转包围盒。
- UI 父子层级目前基于 `ParentEntity`，还不是完整的通用实体 Transform 父子系统。
- UI 父子层级暂不支持在 Hierarchy 中拖拽改父。
- Inspector 还没有属性改动脏标记、批量编辑、组件搜索。
- Viewport UI 编辑还没有独立工具栏，例如移动、缩放、锚点、吸附模式开关。

### 后续路线

1. 增加 UI 对齐工具：左对齐、右对齐、居中、顶部、底部、等距分布。
2. 增加多选和框选：可同时移动、缩放一组 UI 控件。
3. 增加 Hierarchy 拖拽改父：支持 UI 节点拖到 Canvas 或其他 UI 节点下。
4. 增加 Inspector 属性表格化：常用字段固定、扩展字段折叠、长文本区域自动换行。
5. 增加保存提示和脏状态：场景未保存时显示标记，关闭/切换前提醒。
6. 增加 Prefab/模板控件：常用 UI 面板、按钮组、技能栏、背包格可复用。
7. 增加编辑器布局预设：程序、策划、UI、动画、关卡不同工作区。
8. 继续把 VN 和 Side Combat 的第一版专用编辑器升级为更完整的节点/表格工具，并补资源选择器、字段校验和批量编辑。
9. 继续扩展 UUID 对象槽覆盖面，把更多按钮、动画、玩法目标字段做成可选择实体的 Inspector 控件。
10. 继续评估第三方库升级迁移：每次升级前先确认 API 适配成本、构建产物变化和打包体积影响，升级后必须完成全工程构建与核心场景烟测。

### 14. 2026-07-29 稳定 Entity 引用、EnTT v4 与 Scroll/Pager 拆分

- EnTT 已升级到官方 `v4.0.0` single header；旧的 `registry.each` 调用已迁移到基于 `IDComponent` 的 view 遍历。
- EnTT v4 迁移后一度暴露 MSVC 对象文件节数压力；后续已通过第 20 节的大文件职责拆分移除 `/bigobj` 依赖。
- `UIWidgetComponent` 新增 `ParentEntity`，`UIPageItemComponent` 新增 `PagerEntity`。运行时布局和编辑器层级解析统一使用 UUID。
- Inspector 中的 UI 父对象和 PageItem 的 Pager 目标增加对象选择器，日常不再需要手填 Tag；改名后引用不会立即失效。
- Scene Hierarchy 创建 UI 子节点时只写入父节点 UUID，折叠/展开也按 UUID 解析。
- 新增 `UIScrollViewComponent`：用于“不知道总长度、可连续滚动”的内容区，支持鼠标滚轮、可拖动滚动条、内容高度、滚轮步长、滚动条宽度、裁剪配合。
- `UIPagerComponent/UIPageItemComponent` 保持用于“总页数已知、按页切换”的内容区。装备页已迁移到通用 Pager，页码按钮使用 `ui:pager:@<PagerUUID>:page:<n>`。
- 装备背包格继续按“拥有且未装备”的连续列表填格，穿上装备后不会在背包中留下空洞，脱下后会回到背包分页列表。
- `Builds/Windows/Editor/WheatearEditor.exe` 已重新生成；`Builds/Windows/Player/WheatearSandbox.exe` 已重新打包，`content.wtpack` 本轮收集 191 个引用资源。
- 烟测通过：主菜单、装备页、技能树场景在打包版中均能启动并保持运行。

### 15. 2026-07-29 UI 模板创建与 Demo 页面复用

- Scene Hierarchy 右键 `UI / Templates` 新增模板入口，避免策划或程序每次手工搭父面板、滚动条、分页器和 PageItem。
- 新增 `UITemplateCreateCommand`，模板内多个实体作为一个原子命令进入 `CommandHistory`，支持 Ctrl+Z / Ctrl+Y。
- 模板实体使用预生成 UUID 并通过 `CreateEntityWithUUID` 创建，撤销后复原不会破坏子控件父引用。
- `Titled Scroll Text` 面向连续长内容；`Paged Grid` 面向通用分页内容；`Paged Inventory Grid` 面向背包/图鉴/存档槽类固定网格内容。
- 当前竖切 demo 的装备页和存档页已开始使用通用 ScrollView/Pager 思路，后续应继续把 VN 历史、任务列表、图鉴、邮件等页面迁成模板实例或 prefab。
- 已重新构建 `Builds/Windows/Editor/WheatearEditor.exe` 并用它重新打包 `Builds/Windows/Player/WheatearSandbox.exe`。

### 16. 2026-07-29 Side Combat 专用调参面板

- WheatearEditor 新增 `Side Combat Tuning Editor` 独立窗口，可从顶部 `View` 菜单打开，也可在 `SideCombatLevelComponent` 面板点击 `Open Side Combat Tuning Editor` 打开当前关卡的 `Tuning Path`。
- 面板以 `side_combat_tuning.yaml` 为唯一数据源，按 `Feel / Rules / Attacks / Skills / Progression / Raw Preview` 拆分页签。
- `Feel` 面向最常用手感：移动、跳跃、重力、空连滞空、上挑、空中普攻、空中追击和断限追击。
- `Attacks` 面向逐招式参数：hitbox、伤害、前后摇、取消窗口、击飞、hit stun、保护槽、特效帧、音效、hit pause 和震屏。
- `Skills / Progression` 面向技能显示、招式绑定、章节解锁和关卡 profile 可见系统。
- Inspector 中原来的纯文本 YAML 编辑器保留为 `Raw Side Combat Tuning YAML` 折叠项，用于应急修复或手写字段。
- `WheatearEditor` 工程显式链接 `yaml-cpp`，新面板不再把解析逻辑塞进 `EditorLayerBase.cpp`。
- 本轮编辑器重新生成并通过隐藏窗口启动烟测。

### 17. 2026-07-29 编辑器编译瘦身与编码规范

- 根目录新增 `.editorconfig`，统一源码、脚本、文档和场景数据的 UTF-8、缩进、行尾与末尾换行规则，减少中文文档或 YAML 被不同编辑器写坏的概率。
- `WheatearEditor` 工程新增 `src/wepch.h / src/wepch.cpp`，并通过 Premake 的 `pchheader / pchsource / forceincludes` 启用编辑器级预编译头。
- `wepch.h` 只收纳稳定高频头文件：引擎基础 PCH、标准库、glm、ImGui 和 ImGui stdlib 扩展；模块专属头仍放在各自 cpp，避免 PCH 变成新的杂物间。
- `WheatearEditor` 显式链接 `yaml-cpp`，用于支撑 Side Combat 调参面板；`/bigobj` 不作为长期方案保留，后续已通过拆分根治。
- 本轮 Debug 编辑器全量构建通过，耗时约 160 秒；上一轮同级别全量构建约 236 秒，PCH 已经带来明显改善。
- 隐藏窗口启动烟测通过：`Builds/Windows/Editor/WheatearEditor.exe` 运行 6 秒未早退。

后续瘦身重点：

1. `SideCombatSystem.cpp` 体量已经超过 18 万字节，应逐步拆成 tuning 数据加载、动作状态机、命中/受击求解、HUD/反馈表现和场景生成辅助。
2. `GameProgress.cpp / ProgressionSystem.cpp` 已经承载据点、装备、技能树、存档、设置等大量竖切逻辑，正式化时应拆成 SaveGame、Inventory、Equipment、SkillTree、Settings 等独立服务。
3. 编辑器大面板后续继续按“窗口 / 树节点 / Inspector / 命令 / 模板”拆分，避免再次积累成单个超大翻译单元。

### 18. 2026-07-29 大画布 UI 性能规则

- UI 运行时新增通用裁剪剔除：子控件如果完全落在视口或 `ClipChildren` 父面板外，不再参与绘制、文本测量和鼠标命中。
- `UIWidgetLayout` 统一提供 `EntityVisibleInClipAndViewport`，后续编辑器预览、运行时输入和专用 UI 页面都应复用它，避免各系统自己写一套不一致的裁剪逻辑。
- 技能树页面采用“局部可见 + 状态脏标记”策略：只在拖动画布、技能选中、解锁状态变化或重新加载场景时刷新节点布局。
- 技能树刷新内部建立本帧 Tag 索引，替代大量 `FindEntityByName` 线性扫描，作为后续大地图、任务树、图鉴树类页面的性能模板。
- 后续如果继续扩展到几百到上千节点，应在此基础上再做空间网格或四叉树可见集，只实例化/更新可见区域附近的节点。
- 本轮重新构建 `Builds/Windows/Editor/WheatearEditor.exe`，并重新打包 `Builds/Windows/Player/WheatearSandbox.exe`。
- 烟测通过：编辑器、玩家包主入口、玩家包技能树场景均能隐藏窗口启动并保持运行。

### 19. 2026-07-29 原生命令、事件脚本与场景切换服务

- 新增 `CommandBus`：UI 按钮、滑条、事件脚本和模块逻辑可以统一执行 `progression:`、`ui:`、`event:`、`scene:`、`newgame:`、`loadgame:` 等命令。
- 新增 `SceneTransitionService`：`scene:` 类命令不再由脚本直接加载场景，而是转成引擎级结构化切场景请求；`WheatearSandbox` 只作为宿主消费请求并执行加载。
- 新增 `.wts` 原生事件脚本格式，支持 `event`、`wait`、`command`、直接命令、`if/endif` 和基础进度条件。
- 新增 `EventScriptComponent` 与 `EventScriptSystem`，并接入场景序列化、运行时系统注册、复制和编辑器撤销快照。
- Inspector 新增 `Scripting & Audio / Event Script`，可直接设置 `Script Path`、`Start Event`、`Run On Start`、`Run Once`，并内嵌文本编辑 `.wts`。
- 打包器会解析 `.wts` 里的资源引用，并把脚本作为依赖资产加入运行包。
- 新增示例脚本 `assets/events/vertical_slice_flow_sample.wts`。
- ImGui 编辑器字体改为优先加载中文字体并启用完整中文 glyph range，解决 Inspector 中文显示为问号的问题。

### 20. 2026-07-29 移除 /bigobj 与大文件职责拆分

- `SceneSerializer.cpp` 从单文件序列化器拆分为核心调度、组件支持、Core/Scripting/Module/UI/Animation 组件组序列化文件。
- `SceneSerializerComponentSupport.h` 集中 YAML 转换、通用组件序列化模板和基础工具；`SceneSerializerComponentGroups.h` 只暴露组级序列化入口。
- `EditorLayerBase.cpp` 拆分为轻量构造文件、`EditorLayerBaseLifecycle.cpp`、`EditorLayerBaseScene.cpp` 和 `EditorLayerBaseUI.cpp`，分别负责生命周期/输入、场景文件与打包、菜单/Viewport/Canvas UI。
- `SceneHierarchyPanel.cpp` 拆分出 `SceneHierarchyPanelTree.cpp` 和 `SceneHierarchyPanelComponents.cpp`，层级树节点、Prefab/删除菜单与 Inspector 组件绘制不再挤在同一翻译单元。
- `UIEditHandle` 从匿名命名空间提升到 `EditorLayerBase.h` 的编辑器内部枚举，避免核心提交逻辑和 UI 绘制文件各自持有隐式状态定义。
- `Wheatear/premake5.lua`、`WheatearEditor/premake5.lua` 和重新生成的 `.vcxproj` 中均不再使用 `/bigobj`。
- 验证：`Wheatear` Debug x64 单独构建通过；`WheatearEditor` Debug x64 构建通过，并生成 `Builds/Windows/Editor/WheatearEditor.exe`。
- 剩余警告主要是 Mono/第三方库 PDB 缺失警告，属于既有调试符号问题，不影响本轮 `/bigobj` 根治。

### 21. 2026-07-29 竖切 Demo 流程脚本化迁移

- 竖切 demo 新增正式流程脚本 `assets/events/vertical_slice_flow.wts`，不再把主流程散落写在每个按钮和模块组件里。
- 主菜单、VN 序章、VN 假玩法转折、据点、系统页、战斗结算和黑林兽道都接入 `FlowController` 实体。
- UI 按钮、VN 选择项、SideCombat 胜负命令统一改为 `event:<eventName>`，再由 `.wts` 执行保存、读取、等待和切场景。
- `VisualNovelSystem` 的选择项外部命令判断新增 `event:`，键盘选择和鼠标选择都会把事件命令提交给运行时命令链路。
- 这轮迁移后，场景切换路径集中在 `.wts` 中，后续更换流程目标时优先改脚本，不再逐个翻场景 YAML。
- 已重新生成 `Builds/Windows/Player/content.wtpack`，并确认包内包含 `assets/events/vertical_slice_flow.wts`。
- 验证：Sandbox / Editor Debug x64 构建均为 0 错误；直接运行版主菜单、VN 序章、假玩法战斗、正式 SideCombat 烟测通过；打包版入口烟测通过。

### 22. 2026-07-30 战斗后保存与剧情旗标命令

- `GameProgress` 的原生命令新增 `progression:set_flag:<flag>`、`progression:clear_flag:<flag>`、`progression:set_chapter:<n>`，`.wts` 可以直接推进剧情旗标和章节状态。
- `EventScriptSystem` 条件新增 `last_dungeon <dungeonId>` / `last_result <dungeonId>`，用于结算页按上一场副本分流。
- `vertical_slice_flow.wts` 补齐战斗后保存闭环：假玩法胜利、主线 SideCombat 胜利、黑林兽道胜利、结算页回据点都会自动保存一号槽。
- 结算页“再次挑战”改为 `result_retry_last_dungeon`，不再固定回主线战斗；上次打黑林兽道就回黑林兽道。
- 据点“继续剧情”改为 `go_next_story`，通过 `FLAG_CH02_BOSS_DEFEATED` 推进到第三章预告，并设置 `FLAG_CH03_PREVIEW_STARTED`。
- 当前只做核心闭环和骨架，具体教程提示、后续章节复杂分发暂不提前写死，等内容增多后继续迁移。

### 23. 2026-07-30 UI Path 与事件流程图查看器

- 新增 `UIPathComponent`，作为运行时 UI 曲线/路线的通用组件，可用于技能树装饰线、地图路线、任务节点路径和后续图鉴连线。
- `UI Path` 可在 Scene Hierarchy 右键 `UI / Path` 创建，也可以在 Inspector 的 Add Component 中添加；点位、模式、线宽、采样段数和发光都可直接调。
- `UISkillTreeViewComponent` 升级曲线参数：支持线条发光、采样段数、背景环数量、可视化预留边距和更自然的圆环弯曲控制点。
- `VerticalSliceSkillTree.wt` 已更新默认视觉参数，技能树连线更贴合圆形节点，超出面板范围仍由 `ClipChildren` 和 UI 剔除逻辑控制。
- 新增 `Event Script Graph` 编辑器窗口，可从 `View / Event Script Graph` 打开，也可在 `EventScriptComponent` Inspector 中点击 `Open Event Graph` 打开当前 `.wts`。
- 事件图第一版定位为安全查看器：读取 `.wts`，显示事件开始节点、命令节点、等待节点、`if/endif` 条件范围和贝塞尔流程线，点击节点可查看源行和命令内容。
- 事件图暂不直接写回 `.wts`。原因是文本脚本目前更利于 Git 对比和调试，双向蓝图保存需要更完整的节点模型后再做，避免破坏脚本格式。

### 24. 2026-07-30 代码瘦身与默认无 Mono 构建

- 新增 `SceneQueries`，把 VN、Arcade、SideCombat、Progression 中重复的按 Tag 查实体逻辑收回引擎公共层。
- 新增 `UIRuntimeTools`，统一运行时 UI 的显示、文本、进度条、图片颜色/透明度/纹理和按钮 hover 查询，减少玩法模块中的重复工具代码。
- `wtpch.h` 对 Windows 头启用 `WIN32_LEAN_AND_MEAN` 和 `NOMINMAX`，减少全局宏污染和 PCH 负担。
- `premake/common.lua` 新增 `--csharp-scripting` 选项；默认构建不再包含 Mono include 和 Mono 静态库，当前竖切继续走 `.wts + 数据表 + C++ 模块`。
- `ScriptEngine.cpp / ScriptGlue.cpp` 保留 Mono 实现，但只有定义 `WT_ENABLE_CSHARP_SCRIPTING` 时才编译；默认构建使用轻量 no-op stub。
- Editor/Sandbox 的默认 include 路径移除不需要的 `tinyobjloader`，应用层不再继承 Mesh 加载器的第三方搜索路径。
- `.gitignore` 补充 Python `__pycache__` 和 `*.pyc`，避免工具脚本缓存进入 GitHub 仓库。

### 25. 2026-07-30 通用动画事件与 Animation Editor 升级

- `AnimationClip` 新增 `AnimationEvent` 轨道，事件包含触发时间、事件名和命令字符串，并参与 Clip 总时长计算。
- `SpriteAnimatorComponent` 新增 `FireEvents` 开关，运行时可以按实体控制是否触发动画事件。
- `AnimationSystem` 在播放过程中检测事件时间窗口，并把事件命令提交给 `CommandBus`；事件命令支持 `{entity}`、`{clip}`、`{event}` 占位符。
- `CommandBus` 新增 `anim:` 原生命令：`play`、`restart`、`pause`、`resume`、`stop`、`seek`，供 `.wts`、UI 按钮和动画事件统一调用。
- `SpriteAnimatorComponent` 序列化新增 `FireEvents` 和 Clip 内 `Events`，保存场景时会把动画事件一起写入 `.wt`。
- `Animation Editor` 新增事件轨道、事件表格、事件 marker 拖动和右键编辑；双击事件轨道可以快速创建事件。
- `Animation Editor` 的图集生成区升级为 `Spritesheet / Atlas Generator`，明确推荐正式资源使用“一张 spritesheet + 多段 UV 帧”的运行时路径。
- 当前只完成引擎级底座和编辑器能力；现有 Sandbox DEMO 的动画迁移下一轮单独讨论，避免在底层刚升级时一次性改乱内容场景。

### 26. 2026-07-30 Sprite Sheet Picker 与图集子图工作流

- 新增 `Sprite Sheet Picker` 编辑器面板，可从 `Content Browser` 拖入图集，在大预览区按网格点击选择单个 cell。
- 图集选择器支持 `Horizontal Strip / Row Major / Vertical Strip` 三种序列帧切割模式，可一键替换或追加到当前 `AnimationClip`。
- `SpriteRendererComponent` 和 `UIImageComponent` 的 Inspector 增加 `Open Sprite Sheet Picker`，适合 UI 图标 atlas、掉落物图标、静态子图和角色部件选取。
- `UIImageComponent` 新增 `UVMin / UVMax`，UI 渲染改为按 UV 绘制图片；`SpriteRendererComponent` 序列化补齐 `UVMin / UVMax / FlipX`，避免保存后子图选择丢失。
- 默认行原点按视觉左上角计算，更符合 UI 图集和横向动画帧条；保留 top-origin 开关用于兼容旧式 UV 习惯。

### 27. 2026-07-30 Demo 动画桥接到通用 AnimationClip

- SideCombat 不再只依赖模块内置帧播放器：运行时会把 `side_combat_tuning.yaml` 中的 `playerAnimations / gruntAnimations / bossAnimations` 自动转换为 `SpriteAnimatorComponent + AnimationClip`。
- TurnCombat 的 `idle / attack / hit / down` 帧路径也会自动转换为通用 Clip，现有单张 PNG 帧仍可继续使用。
- VN 表情切换增加“同名 Clip 优先”策略：立绘实体如果有 `happy / serious / neutral` 等同名 Clip，就播放动画 Clip；没有 Clip 时保持静态表情差分路径。
- 本轮桥接优先保证现有 demo 不破坏。Hitbox 生成、回合制伤害结算等玩法时机仍由各模块现有逻辑控制；后续更深入的“命中帧事件化”会在动作状态机升级时继续推进。

### 28. 2026-07-30 Component Lifecycle Registry

- 新增 `ComponentLifecycleRegistry`，用 EnTT `on_construct / on_update / on_destroy` 统一分发组件创建、更新和移除事件。
- `Scene` 构造时注册核心生命周期并绑定自身，析构时解绑；后续模块可以把自己的组件初始化逻辑注册到生命周期表，而不是继续塞进 `Scene.cpp`。
- `Entity::AddComponent / AddOrReplaceComponent` 不再手动调用 `Scene::OnComponentAdded<T>`，避免 wrapper 路径和 registry 路径行为不一致。
- `CameraComponent` viewport 初始化已迁移为核心生命周期回调。
- 源码中的旧中文/乱码 `//` 注释已批量清理；后续保留必要英文注释，避免旧编码继续影响 patch 和 review。
- 新文档见 [组件生命周期注册表](组件生命周期注册表.md)。

### 29. 2026-07-31 Gameplay Common 服务层第二批拆分

- `Wheatear/Modules/Common` 新增 Text、Visual、Targeting、UI、UILayout、Reward 六类服务。
- ArcadeCombat 接入共享贴图缓存和标准血条 HUD，假玩法不再自己维护 projectile 贴图缓存。
- SideCombat 接入共享帧路径、数值格式化、贴图缓存和最近敌人查询；动作状态机和 tuning loader 暂留模块内，后续单独拆。
- TurnCombat / TacticalCombat 接入共享命令分割、队伍存活、目标选择和帧路径格式化，回合制/战棋的目标逻辑有了统一参考。
- Progression 的运行时页面搭建函数抽到 `GameplayUILayoutService`，装备页、存档页、设置页继续复用 Pager / ScrollView / Slider 模板。
- 结算页掉落解析抽到 `GameplayRewardService`，tooltip 显示改走 `GameplayUIService`。
- 旧编码损坏的临时 C++ 文案改为 ASCII 占位；正式中文文案后续应进入数据表或本地化资源，避免源码编码再次影响构建。

### 30. 2026-07-31 玩法模块服务层第三批拆分

- `SideCombatSystem.cpp` 拆出 `SideCombatTuningService`，专门负责横板战斗默认参数、YAML 读取、热重载缓存、技能解锁 profile、Boss 保护 HUD 可见性等查询。
- `SideCombatSystem.cpp` 拆出 `SideCombatResultService`，胜利流程不再直接维护评分和经验公式，而是调用统一结算服务刷新 `RuntimeResult*` 字段。
- `ProgressionSystem.cpp` 进一步瘦身为页面调度器，装备页、结算掉落页、存读档页、设置页、技能树页分别拆为 `Progression*PageService`。
- 装备页服务继续保证“已装备物品不出现在背包格子、脱下后按顺序回填、悬浮 tooltip 查看属性、分页按钮走 UUID Pager 命令”。
- 技能树页服务现在集中维护内容表驱动的节点同步、曲线视图数据和局部可见刷新策略；旧实体隐藏与兼容迁移路径已移除，后续大地图、任务树、图鉴树可以参考这个结构。
- 这轮拆分后的原则是：`System` 负责每帧调度和玩法入口，`PageService/TuningService/ResultService` 负责可独立维护的数据、页面、结算与查询逻辑。

---

## 来源：七点打磨记录_2026-08-02.md

## 七点打磨记录 2026-08-02

本轮目标是把玩法架构、编辑器工具和竖切流程继续向“可长期维护的类型化 2D 游戏引擎”推进。原则是：能统一的能力收进服务层，玩法系统只保留循环和状态机；能由数据和事件驱动的流程，不继续散落在场景字段里；编辑器工具要从“能看”升级到“能改、能校验、能预览”。

### 1. Progression 设置职责拆分

- `GameProgress::State` 不再保存 `Settings`，设置状态统一由 `UserSettings` 管理。
- VN 和据点设置命令直接读写 `UserSettings`，避免存档进度和用户偏好出现两份状态。
- 新增 `ProgressionSettingsCommandService`，负责设置命令识别、执行、保存、运行时应用和设置页状态文本。
- `GameProgress` 保留 `progression:` 命令入口，但设置命令的具体逻辑已从进度主文件剥离。

### 2. VN 运行时输入服务

- 新增 `VisualNovelInputService`。
- VN System 不再直接读取鼠标、数字键和输入绑定，而是消费一帧 `InputSnapshot`。
- 这样后续接手柄、改键面板、输入录制/回放或编辑器预览输入时，只需要扩展输入服务。

### 3. 运行时实体引用优化

- 新增 `SideCombatEntityReferenceService`。
- 横板战斗玩家和 Boss 的高频引用改为“首次用作者名解析，运行时缓存 UUID，之后优先按 UUID 查找”。
- `SideCombatSystem` 和 `SideCombatComboService` 不再直接按名字查玩家/Boss。
- 现有 UI 控件名称绑定仍保留为作者层入口，后续逐页迁移为 Inspector 可选实体引用。

### 4. WAO Action Editor 二阶段

- 顶部新增 `Reload YAML`，可热加载 `assets/gameplay/actions` 下的 action YAML。
- Recipe 编辑模式新增 `Effects` 编辑区，支持新增、删除、选择并修改单条效果。
- 单条效果支持编辑类型、属性 ID、状态 ID、信号 ID、数值、回合数、秒数和持续策略。
- 保存 YAML 时会写回 `effects`，不再只保存基础字段。
- 新增 `Validation` 页，检查 ID、时间线、资源路径、资源消耗和效果字段。
- 新增 `Preview` 页，用时间轴展示前摇、命中帧、后摇、取消窗口和资源/信号概要。

### 5. 编辑器大面板拆分

- `AnimationEditorPanel.cpp` 拆出 `AnimationEditorPanelSections.cpp`，承载 spritesheet/atlas、events、frames 三块编辑区。
- `EditorLayerBaseUI.cpp` 拆出 `EditorLayerDockspace.cpp`，默认 Dock 布局构建不再混在主 UI 渲染文件中。
- 后续建议继续把 Canvas overlay 拆为专门编辑工具，但本轮先拆低风险且稳定的大块。

### 6. 战棋流程脚本化

- `VerticalSliceTacticalCombat.wt` 的胜利/失败跳转改为 `event:tactical_combat_victory` 和 `event:tactical_combat_retry`。
- `vertical_slice_flow.wts` 新增战棋胜利事件：设置 `FLAG_CH01_TACTICAL_COMBAT_CLEARED`、保存一号槽、等待后回据点。
- 失败事件统一回到战棋场景重试。
- 以后战棋结算、奖励、剧情 flag 和章节分支都应继续写在 `.wts`，不要散落在场景字段中。

### 7. 当前架构结论

Wheatear 当前更适合继续坚持这套分层：

- `System`：玩法循环、状态机、每帧调度。
- `Service`：具体能力，如输入、目标、行动、命中、奖励、UI、反馈、设置、引用解析。
- `WAO`：动作语义、效果账本、表现信号、调试和轻量 authoring。
- `.wts`：低频流程编排、保存/读取、条件分支、场景跳转。
- `YAML / VN 数据`：高频可调内容、台词、战斗参数、动作配方。

这套结构不是照搬 UE GAS 或 Unity，而是把它们“数据驱动、能力拆分、调试可见”的思想吸收后，变成更适合 Wheatear 当前体量和 2D 类型化玩法目标的工程方案。

---

## 稳定性加固 + 编辑器内容生产闭环 2026-08-16

本轮分两条线：先把会杀进程的稳定性问题清干净，再把"策划必须手写序列化文件"的编辑器缺口全部补上。

### 1. 稳定性加固（数据文件手改不再闪退）

- **序列化全链路加固**：场景 `.wt` / Prefab / `.wtanim` / `.wmat` 反序列化的所有 `as<T>()` 补默认值；`DeserializeEntityFromNode` 实体级 try/catch + 失败实体回滚销毁；`DeserializeYaml`/`DeserializePrefabEntities` 外层兜底；Sandbox 场景加载 try/catch；`main` 全局 catch（异常不再 `std::terminate`）。
- **实体生命周期**：`DestroyEntityImmediate` 加 `valid()` 检查并触发 `OnEntityDestroy`（物理体不再残留/双重销毁）；`StopSystems` 清 `m_SystemCache`（消除 GetSystem 悬垂）；`Scene::Copy`/`DuplicateEntity` 缺组件与物理指针问题；`Entity::operator bool` 查 registry 有效性。
- **渲染/资源**：shader 缓存读取代对齐校验（截断文件不再堆溢出）；wtpack 解包按文件剩余字节钳制分配（损坏包不再 bad_alloc）；OBJ 索引越界跳过；空纹理回落白纹理；1/2 通道纹理支持 `GL_R8/GL_RG`；MSAA+DEPTH32F framebuffer 修正；IBL 缓存截断校验；相机 0 尺寸宽高比守卫；glad/glfw 初始化失败显式报错退出。
- **运行时**：动画 keyframe 解析默认值 + 类型化 cast 修复；动画事件命令收集到帧末执行；`FlushDestroyQueue` swap 批处理（回调入队不再迭代器失效）；`AddExperience` 迭代上限（损坏存档不卡死）；TurnCombat 技能缺失跳过回合（不软锁）；gamesave 槽位解析 catch；`GetCurrentFrame` 帧索引 clamp；UI 递归深度守卫；UserSettings 写入 WritableRoot（打包版按键设置不再随缓存丢失）。
- **音频**：SFX 从"双槽共享 data source"改为"每播放独立 buffer + 共享不可变 PCM"，消除快速连发时的游标竞争；解码 reserve 上限 + 头信息合法性校验。

### 2. 编辑器内容生产闭环（策划不手写任何数据文件）

- **通用数据文件编辑器 `DataFileEditorPanel`**：任何 `.yaml/.json/.wtsettings/.wtanim` 双击即打开；YAML 结构化树编辑（共享组件 `YamlTreeEditor`，含类型化资产选择器）+ 可编辑 Raw + 保存前校验 + 树编辑 32 步撤销。
- **rect 可视化编辑**：`rect: [x,y,w,h]` 字段渲染为迷你画布可拖拽方框（移动/缩放），适用所有含 rect 的 YAML 数据（如 `ui_atlas_params.yaml` 的 VN UI region）。
- **`.wtsheet` 命名矩形**：Sprite Sheet Picker 加载/保存 rects（此前保存即丢数据），新增命名矩形列表编辑 + 画布橙色叠加；Sprite Renderer / UI Image Inspector 增加图集路径 + 格号 + 命名矩形下拉。
- **新建入口**：Content Browser 右键新增 `New VN Script (.vn)`、`New Animation Clip (.wtanim)`（带模板自动打开）。
- **Raw 兜底**：VN / SideCombat Tuning / Progression / Alias Manifest 的只读 Raw 预览下方新增 `Edit in Data File Editor` 跳转按钮。
- **资产定位高亮**：资源引用字段旁新增 `Locate` 按钮，Content Browser 自动导航 + 选中 + 橙色高亮闪烁（`ContentBrowserRequests` 轻量请求通道）。
- **VN 图集热重载**：`VNUiAtlasRegions` 改为写时间探测缓存（500ms），`ui_atlas_params.yaml` 保存后游戏内即时生效。

### 3. Content Browser UE 化

- 左/右侧栏改为可拖拽分割条（握把线常显 + 缩放光标）。
- 左侧 Sources 面板：根节点 `All Assets` + 文件夹树。
- 工具栏 `Filter` 按钮：弹出勾选菜单按类型过滤（替代常驻 chips 行，省空间）。
- 滚轮缩放缩略图（Ctrl+滚轮任意状态；内容一屏放下时纯滚轮也可）。
- 资产名固定绘制在缩略图下方（不再依赖光标状态）；图标间距加大。
- 清理面板英文说明（PanelHeader 副标题删除、其余本地化）。

### 4. 架构结论（延续）

数据文件的"类型化面板 + 通用树编辑器"双轨继续成立：高频字段走类型化控件，任意新键走通用树兜底，两者共用同一份 YAML 根节点与保存管线；通用能力（YamlTreeEditor、DataFileEditorPanel、ContentBrowserRequests）放公共层，玩法专属面板只做字段编排，换项目不换工具。

## 编辑器闭环深化 + WAO 行为数据化 2026-08-16（第二轮）

在"内容生产闭环"基础上，把"做新玩法要改 C++"的边界逐层打开。目标检验：
**纯编辑器能不能重做 Sandbox**（差距分析见 `docs/02_竖切版本/` 与 Part 10 的 10.9/10.10）。

### 1. 输入动作数据化

- `assets/input/action_bindings.yaml`（引擎内置 + 项目覆盖）：动作 ID/默认键/中英文名全数据。
- `InputActionCatalog`：加载/合并/键名解析；`InputBindingService::GetKeys` 回退链
  改为「用户重映射 → Catalog → C++ 内置表 → 空」；`ResetToDefaults` = 清空用户重映射。
- `InputBindingsPanel` 重写：分组 + 显示名 + **新增动作/绑键/停用恢复**（写回项目文件）。
- 修复 `side.launcher` 无默认键的历史 bug。

### 2. 实体绑定 @UUID 双轨

- `Scene::GetEntityByName` 单点支持 `@UUID`/`uuid:`/`id:` 前缀——全部 50+ 按名绑定自动升级；
  旧场景名字绑定完全兼容。
- `DrawSceneEntityField` 选择器：选中写 `@UUID`、显示当前名字、Missing 徽标——**重命名不断链**。
- Prefab 实例化重映射扩展至全部 6 类玩法组件的绑定字段；修复 F2 重命名把 `[UI] ` 前缀写入 Tag 的 bug。

### 3. 三战斗模块全局调参（参照 SideCombat 模式）

- Arcade / Turn / Tactical 各一套 `XxxCombatTuningService`（500ms 热重载 + 字段缺失回退）
  + 独立调参面板（Level/Formula/Board/Player 等标签页 + Advanced 树 + Raw 兜底）。
- 伤害公式系数数据化（Turn 防御系数、Tactical 魔法防御系数）。
- 每个面板新增「场景单位」标签页：当前场景单位属性**可编辑**（拖拽一次一个撤销步），
  全部场景文件只读总览表（文件级扫描，不依赖组件类型）。

### 4. 运行时调试

- 引擎日志 ring-buffer sink（`Log::DrainEditorMessages`）+ 编辑器 Console 面板（级别过滤/搜索/自动滚动）。
- Play 模式 Ctrl+点击视口拾取实体；Properties 顶部显示「播放（运行副本）」徽标。

### 5. UI 画布编辑

- 边缘/中心吸附（青色参考线）、Ctrl 网格吸附、16×9 网格叠加、参考像素标尺（顶/左）、Snap/Grid 开关。
- 编辑器文件夹分组（`EditorFolderComponent` 可序列化、运行时忽略）：拖入/移出/防环/撤销/创建子文件夹。

### 6. WAO 行为数据化（效果/道具槽/技能槽）

- `itemSlots` / `skillSlots` 表驱动（`side_combat_tuning.yaml`）：道具槽与技能槽可增删，
  新热键 = 加行 + 输入绑定加动作；`side:item:N` 命令路由查表映射。
- `EffectSpec` 新增 `formula`（表达式求值器 `EffectFormula`：`+ - * / %`、比较、逻辑、
  `min/max/clamp/abs/round/floor/ceil/if`，变量 = 属性字典）与 `customType`
  （效果注册表 `EffectRegistry`：一次注册全局可用，编辑器下拉自动列出）。
- 技能行为注册表 `SideCombatSkillRegistry`：kind=custom + Behavior 下拉。
- 示例：lifesteal / mana_leech / full_heal / berserk（狂化）。

### 7. 未做与边界（见 Part 10 · 10.10 弹反讨论）

- 新"行为类型"（跨实体交互 + 持续状态，如弹反）仍需要一次 C++ 注册（约 40 行），
  之后编辑器调参；纯编辑器化需要"可挂起行为状态机 + 实体查询原语"，属于下一里程碑。
- 打包版 `action_bindings.yaml` 尚未进 wtpack 依赖扫描（打包版回退 C++ 内置表，行为不变）。

## 硬缺口修复 + 敌人波次数据驱动 + 编辑器生产闭环深化 2026-08-16（第三轮）

在"纯编辑器重做 Sandbox"差距分析基础上，把剩余硬缺口与大工程逐项落地。目标检验：
**新关卡不需要拖敌人进视口、不需要手写数据文件、单位数值批量可改。**

### 1. 启动场景项目级配置

- `assets/game/player.config` 成为项目级配置：Project Health 面板可读写（"保存启动场景"按钮），
  打包弹窗/CLI 无显式参数时从项目配置读取；新建项目自动生成默认配置。
- dev 模式（`--project`）运行时同样读取项目配置（cwd 链未命中时回退项目根）。

### 2. 支援角色命令表驱动化

- `progression:select_support:<id>` 按 `relationships.yaml` 查表；旧 4 命令名保留别名
  （`guard`→`shield_guard` 顺带修正 id 不一致）。
- 新角色加进 yaml 即自动可用：未解锁显示占位消息，已解锁直接生效。

### 3. 装备升级配方数据化

- `upgrades.yaml` 从"只读写死 2 个 key"改为整表驱动，新增 `equipmentId/name/targetLevel/
  goldCost/resultMessage/notification` 字段；新命令 `progression:upgrade_item:<id>`。
- 存档新增 `equipmentLevels`（旧档兼容，旅人护衣等级回退 legacy 字段）；装备页按钮按
  选中装备的配方动态生成，新配方无需改场景；编辑器升级面板补齐新字段（含金币消耗）。

### 4. Arcade 弹幕参数化

- Boss 跳跃几何（频率/振幅/弧高/边缘收缩）、子弹弹道、三把武器弹道全部移入
  `arcade_combat_tuning.yaml`（默认值 = 原常量，零行为变化）；Tuning 面板补全
  Boss/Weapons 页签；热重载对下一次跳跃/射击即时生效。

### 5. 资产导入与新建入口

- `FileDialogs::OpenFiles` 多选文件对话框；Content Browser「导入资源...」（工具栏/右键）
  复制去重 + 自动 Rescan。
- 右键新增 `New Scene (.wt)`（相机+UI Canvas+存档策略模板，创建即打开）、
  `New Sprite Sheet (.wtsheet)`、`New Data File (.yaml)`。

### 6. 小坑合集

- 事件脚本图默认值不再写死 VerticalSliceHub 演示场景（Scene/Flag/Dungeon 节点均为中性占位）。
- 打包弹窗场景路径非法时 Package 按钮禁用。
- Play 模式禁用 Add/Reset/Remove 组件（结构编辑保护，数值调试保留）。
- 新建项目模板补 UI Canvas + SavePolicy。

### 7. SideCombat 敌人波次数据驱动（新需求）

- `SideCombatLevelComponent.WaveSpawns` 波次表：类型（Grunt/Thrower/Pouncer）+ 数量 +
  出生 X 范围 + 地面偏移 + HP 浮动；Inspector 直接编辑（撤销栈）。
- `side_combat_tuning.yaml` 新增 `enemyTypes` 模板表（数值/AI 参数/渲染与阴影缩放），
  Tuning 面板新增「敌人种类」页签。
- 运行时 `SpawnWavesFromTable`：表非空时移除场景静态小兵（Boss 保留）并按表生成
  （含影子实体、HP 抖动、波次激活、动画桥接全走既有管线）。
- `SideCombatBeastPath.wt` 已迁移：删除 270 行静态敌人实体，改为 2 条波次记录
  （波1×2 + 波2×3），运行日志验证 `spawned 5 enemy(ies)`。
- Turn/Tactical 边界：敌人定义已可在编辑器调参（Scene Units + 批量表）；数量由战斗设计
  固定且每敌一套 UI 绑定实体，生成器收益低，暂不做。

### 8. 战斗单位批量编辑（CombatUnitOverview 可写化）

- 四个战斗 Tuning 面板的「场景单位」总览表单元格全部可编辑，回车写回场景文件
  （场景 .wt 无手写注释，yaml 重写零损失）；bool/int/float/string 自动推断。

### 9. UI 画布多选与对齐

- 画布编辑器 Ctrl+点击多选（黄色高亮），场景层级选中保持为对齐基准。
- 工具栏新增 L/H/R/T/V/B 六向对齐 + DH/DV 等距分布；每个操作一条撤销记录
  （父区域归一化坐标换算，跨父控件同样正确）。

### 10. 边界结论

- EntityName→@UUID：双轨已完整（运行时 `GetEntityByName` 单点支持 @UUID 选择器+名字，
  编辑器选择器写 @UUID），存量名字绑定保持兼容，无需批量迁移。
- 三模块 HUD 预设：Turn/Tactical/Arcade 布局已由场景 UI 实体 + 画布编辑器覆盖；
  SideCombat 的 HUD 预设 yaml 是特有机制，引入需布局数据化重构，暂不做。
- YAML 注释保留：yaml-cpp 无注释输出能力，需换序列化库（ryml）或逐行 patch 管线，
  属架构级改动，列为独立里程碑。
- 新"行为类型"（弹反类跨实体交互）仍须一次 C++ 注册，见 Part 10 · 10.10。

## SideCombat HUD 预设退役 2026-08-16（第三轮收尾）

- 删除 `side_combat_hud_preset.yaml`、`SideCombatHudPresetEditorPanel`、`Apply/Save/DefaultPath`
  与死开关 `HudPresetOverridesEnabled`（运行时零消费）。
- 预设数据已烘焙进 `SideCombatBeastPath.wt` 的 `SideCombatLevelComponent`（85 个键：
  实体名绑定/布局/槽位/文案），场景自包含；序列化改为无条件输出全部 HUD 字段。
- 保留 `CaptureSceneLayout`（运行时把场景实体布局同步回组件字段 + Inspector「捕获场景 HUD 布局」按钮）。
- 结论依据：预设 yaml 是场景 HUD 数据的唯一存储（反序列化时无条件 Apply 填充），
  布局/槽位/文案均被运行时消费，删除前必须迁移；迁移后 yaml 层冗余，可安全移除。

## CaptureSceneLayout 退役 2026-08-16（第三轮收尾二）

- `SideCombatHudPreset`（CaptureSceneLayout）整体删除：HudService 运行时调用、Inspector「捕获场景 HUD 布局」按钮、
  抽屉状态显示、引擎文件全部移除。
- 删除依据（查证）：运行时布局消费点中 `JoystickBase`/`ComboFrame` 已实体优先（实体位置覆盖字段），
  `SkillTooltip` 是动态浮动提示（字段驱动为设计意图，字段=场景内值，删除 Capture 行为不变）；
  其余布局字段经 `EnsureUIWidget`（`(void)position`）运行时零消费。
- 至此 HUD 布局数据单一来源 = 场景 UI 实体（画布编辑器摆放），组件字段布局值仅作兜底/信息。
