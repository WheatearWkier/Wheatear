# 《Wheatear 异世界项目》竖切工程实现记录 v0.9

## 0. 文档定位

本文档记录竖切版本在工程中的实际落地位置，避免策划文档、代码模块、场景资源和临时美术脱节。

当前竖切目标：

`主菜单 -> 序章 VN -> 第一章假玩法 -> 第二章正式横板战斗 -> 战斗结算 -> 据点 UI -> 魔剑技能树 / 装备强化 / 副本选择 / 好感关系 / 支援配置 / 存读档 / 设置 -> 重刷副本 / 继续剧情`

## 1. 工程模块

### 1.1 新增运行时模块

| 模块 | 路径 | 职责 |
| --- | --- | --- |
| `SideCombat` | `Wheatear/src/Wheatear/Modules/SideCombat/` | 正式横板战斗，不进入引擎基础 `Systems` |
| `Progression` | `Wheatear/src/Wheatear/Modules/Progression/` | 竖切阶段的进度、材料、经验、魔剑升级、据点状态刷新 |

`SideCombat` 当前组件：

| 组件 | 职责 |
| --- | --- |
| `SideCombatLevelComponent` | 关卡边界、纵深战斗带、调参表路径、UI 绑定、胜负跳转、连击和奖励运行态 |
| `SideCombatantComponent` | 玩家和敌人的生命、攻防、脚下碰撞、空中高度、浮空速度、硬直、受控状态、Boss 保护条运行态 |
| `SidePlayerControllerComponent` | 一段跳、横向推进、纵深走位、普攻、上挑、魔法弹、支援冷却、魔剑槽运行态 |
| `SideEnemyAIComponent` | 小怪 / Boss 基础 AI、纵深追踪、攻击距离和纵深容差 |
| `SideHitboxComponent` | 攻击判定、伤害、空中命中区间、浮空速度、硬直、Boss 保护增长、特效序列帧 |
| `SidePickupComponent` | 掉落材料、吸附半径、拾取速度 |

`Progression` 当前职责：

| 文件 | 职责 |
| --- | --- |
| `GameProgress.*` | 竖切进度：等级、经验、属性、材料、首通/重刷、副本解锁、魔剑 Lv2、旅人护衣 +1、好感关系、支援选择、设置、存读档 |
| `ProgressionSystem.*` | 据点、结算、技能树、装备、副本选择、好感关系、支援配置、设置、存读档页的文本和进度条刷新 |

`UIAnimatorComponent` 由 `UISystem` 统一驱动，负责竖切页面的淡入、滑入、结果弹出、循环呼吸和按钮悬停缩放。

注意：当前 `Progression` 是竖切可玩闭环，不是最终存档格式。未来正式化时应继续拆出持久化 `SaveGame`、背包 `Inventory`、装备实例 `Equipment` 和好感 `Relationship`，但据点 UI 可以先按这套接口跑通。

### 1.2 编辑器接入

| 路径 | 说明 |
| --- | --- |
| `WheatearEditor/src/Modules/SideCombat/SideCombatDrawer.*` | SideCombat 组件属性面板 |
| `WheatearEditor/src/Modules/ModuleEditorBootstrap.cpp` | 注册 SideCombat 组件 |

编辑器现在可以添加和保存 SideCombat 组件。后续做关卡编辑时，不需要把正式横板战斗参数硬写在代码里。

### 1.3 序列化和运行时跳转

已接入：

- `Scene::Copy` 组件复制白名单。
- `SceneSerializer` 保存和读取白名单。
- `ModuleBootstrap` 运行时系统注册。
- `RuntimeSceneLayer` 读取 `SideCombatLevelComponent.RuntimeRequestedCommand`。
- `RuntimeSceneLayer` 处理 `progression:` 据点按钮命令。

这四处缺一处，竖切都可能出现“编辑器能看见但运行丢失”或“战斗胜利但无法跳场景”的问题。

## 2. 场景链路

| 场景 | 文件 | 说明 |
| --- | --- | --- |
| 主菜单 | `assets/scenes/VisualNovelMainMenu.wt` | 开始游戏进入竖切入口 |
| 序章 VN | `assets/scenes/VerticalSliceIntro.wt` | 现实上学路、穿越、假青梅被掳走 |
| 第一章假玩法 | `assets/scenes/VisualNovelBattle.wt` | 保留现有 ArcadeCombat，用作剧情误导 |
| 假玩法后 VN | `assets/scenes/VerticalSlicePostFake.wt` | 导师送魔剑，引出正式横板 |
| 正式横板战斗 | `assets/scenes/SideCombatVerticalSlice.wt` | 黑熊丈夫 Boss |
| 据点 UI | `assets/scenes/VerticalSliceHub.wt` | 结算、重刷、技能、装备、副本、好感、支援、存档、设置入口 |
| 第三章预告 | `assets/scenes/VerticalSliceChapter3Preview.wt` | 竖切结束点 |
| 战斗结算 | `assets/scenes/VerticalSliceResult.wt` | 显示评分、经验、掉落和回据点 / 重刷入口 |
| 魔剑技能树 | `assets/scenes/VerticalSliceSkillTree.wt` | 分支图标节点、选中详情、技能材料和学习入口 |
| 装备强化 | `assets/scenes/VerticalSliceEquipment.wt` | 装备槽、背包分页、选中详情、强化和属性查看 |
| 副本选择 | `assets/scenes/VerticalSliceDungeonSelect.wt` | 黑林兽道材料本入口、推荐信息、掉落和评分状态 |
| 黑林兽道 | `assets/scenes/SideCombatBeastPath.wt` | 第二章可重刷材料副本，使用 `CH02_MAT_BeastPath` profile |
| 好感关系 | `assets/scenes/VerticalSliceRelationship.wt` | 导师、白魔法、剑盾、黑魔法、王妃等关系状态 |
| 支援配置 | `assets/scenes/VerticalSliceSupport.wt` | 当前主动支援选择、锁定原因和支援收益 |
| 保存读取 | `assets/scenes/VerticalSliceSaveLoad.wt` | 竖切进度保存 / 读取入口，和 VN 存读档覆盖层使用同一套一号槽 |
| 系统设置 | `assets/scenes/VerticalSliceSettings.wt` | 文本速度、音量、震屏、全屏偏好状态和滑条显示 |

以上 WheatearSandbox 主流程相关场景已经统一补入 `WT_UI_Canvas` 根画布；原有 UIWidget 都挂到该 Canvas 下。后续用 WheatearEditor 微调菜单、VN、结算、据点、技能树、装备和设置等 UI 时，优先在 `UI Canvas Editor` 中编辑，避免再生成无 Canvas 归属的孤儿 UI。

## 3. 当前战斗输入

| 输入 | 行为 | 设计目的 |
| --- | --- | --- |
| `A / D` 或左右方向键 | 左右推进 | 横向推进和追击 |
| `W / S` 或上下方向键 | 纵深走位 | 接近《机甲旋风》式横向清场视角 |
| `K` / `Space` | 一段跳 | 躲避远程攻击，也可接空中连击；正式文案以 `K` 为准，`Space` 仅保留测试兼容 |
| `J` 或鼠标左键 | 地面普攻三段；空中跳斩 | 每段都能续连，跳斩负责基础空中续连 |
| `S + J` | 地面裂空上挑；空中追斩 | 主要浮空起手，空中用于续住快下落的目标 |
| `U` | 魔法弹 | 远程续连和补刀 |
| `I` | 真青梅支援 | 较长冷却的浮空续连 |
| `L` | 竖切断限追击调试键 | 当前默认隐藏 UI 提示，只用于测试后期断限底层能力 |
| `Esc / P` | 暂停切换 | 调试和试玩用 |

竖切暂不开放普通二段跳。未来“断限追击”会作为高手机制重置空中行动，而不是常驻二段跳。

战斗 HUD 只显示 `S+J / U / I / L`。`J` 普攻和 `K` 跳跃是基础动作，不进入技能栏；后续 UI 空间紧张时优先使用图标和悬浮 tooltip，不把长说明塞进技能槽。

### 3.1 当前战斗视角实现

v0.2 已从“平台式 X/Y 重力”改成“横向推进 + 纵深战斗带 + 独立空中高度”：

- `RuntimeGroundPosition.x` 表示左右推进。
- `RuntimeGroundPosition.y` 表示地面纵深站位。
- `RuntimeAirHeight` 表示浮空高度，不再把角色当作平台游戏那样直接改地面 Y。
- 攻击判定同时检查 X 距离、纵深距离和空中高度区间。
- 角色脚下阴影会根据浮空高度淡出，帮助玩家看清 Boss 是否真的被挑起来。

这套结构是后续“浮空连击、断限追击、Boss 保护槽”的基础。普通玩家可以地面立回通关，高手玩家可以研究如何把 Boss 留在空中。

## 4. 资源目录规范

所有竖切专用资源放在：

```text
WheatearEditor/assets/vertical_slice/
```

当前目录：

| 目录 | 内容 |
| --- | --- |
| `vn/portraits/` | VN 立绘和表情差分 |
| `vn/backgrounds/` | VN 背景 |
| `ui/panels/` | 结算页、据点页、技能树页、装备页背板 |
| `ui/icons/` | 结算、技能、装备、教程、材料、副本、关系、支援、设置、存档图标 |
| `ui/nodes/` | 技能树节点图标 |
| `side_combat/characters/` | 横板玩家角色 |
| `side_combat/enemies/` | 横板敌人和 Boss |
| `side_combat/effects/` | 攻击、魔法、支援、敌人特效序列帧 |
| `side_combat/ui/` | 材料、魔剑、UI 图标 |
| `data/` | 竖切资源清单和后续数据输入 |

资源清单：

```text
WheatearEditor/assets/vertical_slice/data/vertical_slice_manifest.json
```

正式横板战斗调参表：

```text
WheatearEditor/assets/vertical_slice/data/side_combat_tuning.yaml
```

战斗调参优先使用 WheatearEditor 的 `Side Combat Tuning Editor`，不要直接改 C++。必要时仍可手动编辑 `side_combat_tuning.yaml`，因为 YAML 仍是运行时唯一数据源：

- `player`：玩家移速、一段跳高度、重力、空中控制、普攻 / 上挑 / 魔法 / 支援 CD、连招窗口。
- `combat`：连击保留时间、受击无敌、防御减伤公式、最小伤害。
- `airCombo`：空中动作次数、跳斩 CD、空中追斩 CD、断限冷却、断限窗口、低空威胁线、高空安全线。
- `protection`：Boss 保护条上限、保护衰减、满槽脱离时间、断限压回保护条、保护 HUD 调试开关。
- `enemy`：敌人追击速度、攻击距离余量、Boss 血量阶段、冲撞和震波触发距离。
- `pickup`：掉落拾取半径、吸附半径、吸附速度。
- `movement`：纵深边界、纵深速度、排序尺度。
- `visuals`：脚下阴影透明度和浮空淡出。
- `reactions`：Boss 受击浮空修正。
- `attacks`：每个招式的判定大小、偏移、伤害、硬直、浮空力度、特效序列帧、帧率。
- `skills`：技能定义，记录技能名、输入、连招职责、关联攻击 ID、首次解锁章节和是否核心动作。
- `progression.profiles`：章节解锁配置，按 `LevelId` 决定本场战斗开放哪些技能、哪些机制可见、哪些后期能力仅供开发调试。

编辑器入口：

- 选中带 `SideCombatLevelComponent` 的实体，在 Inspector 点击 `Open Side Combat Tuning Editor`。
- 或者从顶部菜单 `View / Side Combat Tuning Editor` 打开。
- `Feel` 页签调跳跃、重力、滞空、上挑、空中普攻、空中追击和断限追击。
- `Rules` 页签调战斗规则、Boss 保护槽、敌人 AI、拾取物、纵深移动和反馈。
- `Attacks` 页签逐招式调 hitbox、伤害、前后摇、取消窗口、击飞、特效和音效。
- `Skills / Progression` 页签调技能定义、技能开放和关卡 profile。

调跳跃高度优先改：

```yaml
player:
  jumpImpulse: 7.8
  gravity: 20.0
```

`jumpImpulse` 越高，起跳越高；`gravity` 越高，下落越快。竖切仍保持常规一段跳，未来只有“断限追击”会临时刷新空中行动。

运行时读取规则：

- 编辑器或直接运行 Sandbox：战斗场景启动时读取工程内的 `side_combat_tuning.yaml`。
- 打包开发版：打包器会把调参表同时写入 `content.wtpack`，并复制一份 loose 文件到 `Builds/Windows/Player/assets/vertical_slice/data/side_combat_tuning.yaml`。
- 打包运行时优先读取 loose 调参表；没有 loose 文件时才读取包内资源。
- 调参缓存会检查文件修改时间。改表后重进战斗场景即可稳定生效；攻击判定类参数会在下一次生成攻击时读取到新值。
- `attacks` 下新增动作手感参数：`startup` 前摇、`lifetime` 命中框持续、`recovery` 后摇、`cancelWindowStart / cancelWindowEnd` 取消窗口、`movementScale` 出招移动倍率。

第二章竖切的机制开放边界：

- `breakLimitEnabled: false`，断限追击不作为第二章正式玩法开放。
- `breakLimitDebugKeyEnabled: false`，避免全局绕过章节配置。
- `progression.profiles.CH02_MAIN_BearAwakening.debugSkills: [break_limit]`，只在第二章竖切 profile 下保留 `L` 键方便开发测试断限底层逻辑。
- `showBreakLimitHint: false`，默认不向玩家提示断限。
- `showBossProtectionHud: false`，默认不把 Boss 保护条显示成正式 UI。
- `showCombatStateHud: false`，默认不显示 Boss 内部运行状态。
- Boss 保护条仍然在后台运行，用来防止教程 Boss 被无限浮空；第六章再显性教学保护条，第七章再正式开放断限追击。

当前 profile 规划：

| Profile | 章节 | 正式开放 | 隐藏调试 | 机制显示 |
| --- | --- | --- | --- | --- |
| `CH02_MAIN_BearAwakening` | 第二章 | 三段斩、跳斩、上挑、空中追斩、火球、真青梅支援 | `break_limit` | 不显示保护条，不提示断限 |
| `CH02_MAT_BeastPath` | 第二章材料本 | 同第二章主线 | `break_limit` | 不显示保护条，不提示断限 |
| `CH06_MAIN_MageLair` | 第六章 | 基础技能 | 无 | 显示 Boss 保护条 |
| `CH07_MAIN_PalaceAssault` | 第七章 | 基础技能 + 断限追击 | 无 | 显示 Boss 保护条，提示断限 |

后续替换正式美术时优先保持同名同路径，避免重新改场景、脚本和打包引用。

## 5. 临时美术生成

VN 临时素材由脚本生成：

```text
tools/vertical_slice/generate_vertical_slice_assets.py
```

正式横板战斗临时素材由脚本生成：

```text
tools/vertical_slice/generate_side_combat_assets.py
```

竖切 UI 临时素材由脚本生成：

```text
tools/vertical_slice/generate_vertical_slice_ui_assets.py
```

系统完整性页面和文字样式由脚本生成 / 更新：

```text
tools/vertical_slice/generate_system_complete_ui.py
```

系统完整性后的图标化 UI 打磨由脚本生成 / 更新：

```text
tools/vertical_slice/generate_ui_polish.py
```

用途：

- 提供可运行、可打包、可替换的占位 PNG。
- VN 立绘提供 `neutral / happy / serious / surprised / thinking` 五种表情。
- 横板战斗提供玩家、Boss、小怪、战斗带背景、脚下阴影、技能特效序列帧、掉落图标。
- 竖切 UI 提供结算页、据点页、技能树页、装备页、副本选择、好感关系、支援配置、存读档和设置页的面板、图标和节点占位图。
- `generate_system_complete_ui.py` 同时会把竖切场景中的关键中文 UI 文本写入阴影和描边字段，保证打包版文字可读性。
- `generate_ui_polish.py` 负责生成技能、装备小图标，并把技能树、装备页、设置页、存读档页和 VN 存读档覆盖层更新成更接近正式游戏的交互结构。

注意：

- 这些图是程序化占位图，不是最终美术质量。
- 生成脚本保留在工程内，是为了保证任何人能重新生成同一批临时资源。

## 6. 当前实现边界

已实现：

- 正式横板战斗模块骨架。
- 横向推进、纵深走位、一段跳、普攻、上挑、魔法、支援。
- 玩家攻击动作时序层已接入：技能先进入动作状态，再按调参表的前摇、命中框、后摇和取消窗口生成攻击，避免所有技能“按键即判定”。
- 空中 `J` 已拆为基础跳斩 `air_basic`，地面 `J` 仍是三段斩。
- `K` 已回归一段跳；`S + J` 指令拆为地面 `launcher` 和空中 `air_chase`。
- 玩家跳跃加入 `jumpBufferTime` 和 `coyoteTime` 两个手感参数，移动中按跳、落地前后按跳更不容易丢输入。
- 竖切断限测试键 `L` 已接入：满足连击和下落窗口时，刷新一次跳跃和空中动作次数。
- 空连高度资源已接入：空中命中会维持连招但逐步下坠；低空空连可被地面怪打断，高空空连可避开普通地面怪。
- 浮空控制规则已接入：非霸体敌人被击飞后，在落地前不移动、不攻击，保持受击表现；未来只有明确配置的空中反击 / 霸体技能可以例外。
- 受控状态机已接入：`Normal / HitStun / Launched / Knockdown / Recovery / SuperArmor / Broken / Dead`。
- Boss 隐藏保护条已接入：玩家连续控制 Boss 会积累保护，满槽后 Boss 强制脱离连击并进入短暂保护恢复。
- 魔剑槽已接入：命中可回复魔剑槽，断限调试键会消耗魔剑槽。
- 断限追击已做成可配置机制：正式开关、调试键、UI 提示和保护 HUD 分离，便于按章节解锁。
- 章节 / 技能解锁数据层已接入：运行时按 `SideCombatLevelComponent.LevelId` 查 `progression.profiles`，未解锁技能不会响应输入，调试技能可用但不会进入玩家 HUD。
- 三维判定模型：X 距离、纵深距离、空中高度区间。
- 攻击特效序列帧播放。
- 伤害、防御、硬直、浮空、连击计数。
- 敌人攻击动作时序层已接入：Boss 爪击、震波、冲撞都有可配置前摇和后摇；敌人被浮空或硬直打断时，未完成的攻击动作会取消。
- Boss 基础三阶段倾向：普通爪击、中血震波、低血冲撞。
- Boss 死亡掉落材料，材料向玩家吸附。
- 掉落拾取会写入 `GameProgress`，据点可以读取真实材料数量。
- 胜利结算会根据用时、最佳连击和受击次数生成评价，并记录首通/重刷经验、剧情标记和黑林兽道解锁。
- `Progression` 竖切内存进度已接入：等级、经验、属性、材料、通关记录、魔剑 Lv2、旅人护衣 +1。
- `ProgressionSystem` 已接入结算页、据点页、技能树页和装备页的文本与进度条刷新。
- `UIAnimatorComponent` 已接入 `UISystem`，竖切页面支持淡入、滑入、结果弹出、循环呼吸和按钮悬停缩放。
- 胜利后跳转战斗结算页，再从结算页回到据点 / 重刷副本 / 打开技能树。
- 据点 UI 可继续到第三章预告。
- 据点技能按钮现在跳转技能树页，装备按钮跳转装备页，升级动作放在对应页面中完成。
- 据点首页已扩展为完整系统入口：副本选择、魔剑技能树、装备强化、好感关系、支援配置、保存读取、系统设置、继续剧情。
- 副本选择页已能进入 `SideCombatBeastPath.wt`，黑林兽道结算后返回结算 / 据点闭环。
- 好感关系页已读取导师、白魔法、剑盾、黑魔法、王妃等关系记录，显示好感、支援等级、锁定状态和事件提示。
- 支援配置页已能切换当前主动支援，锁定角色会显示锁定原因。
- 保存 / 读取页已能写入和读取 `assets/saves/progression_slot1.wtsave`，主菜单继续游戏会同步读取竖切进度。
- VN 指令栏的保存 / 读取现在会打开 `VN_SaveLoadPanel` 覆盖层，玩家可以在 VN 内保存或读取一号槽；`vn:save` / `vn:load` 会同时处理 VN 剧情位置和 `GameProgress` 成长进度。
- 设置页已接入文本速度、音量、震屏和全屏偏好状态；其中 VN 文本速度已经真实影响 `VisualNovelTextComponent`。
- 魔剑技能树页已从横向文字列表改为分支图标节点网络，节点点击后刷新 `SkillTree_Details`、材料需求和学习按钮状态。
- 装备页已从单装备说明改为装备槽 + 背包图标网格 + 分页按钮 + `Equipment_PageSlider` + 选中详情面板；当前竖切强化仍以旅人护衣作为可操作样例。
- 设置页已增加 `UISliderComponent` 可视化滑条，用于显示文本速度、主音量、BGM 音量和音效音量。
- `UITextComponent` 已增加 `ShadowColor`、`ShadowOffset`、`OutlineColor`、`OutlineThickness`，UI 渲染器按阴影、描边、正文顺序绘制，解决细字发瘪和浅色背景上不清晰的问题。

暂未实现：

- 正式多槽存档系统和多槽 UI 细节，目前 VN、据点和战斗后结算共用竖切一号槽。
- 完整背包、装备实例和词条系统。
- 多房间材料副本。
- 断限追击完整正式内容，包括断限失败惩罚、连续断限递增难度、章节解锁演出和正式教学流程。
- 敌人配置表自动导入。
- 设置页中的音量已经接入 VN BGM、战斗 BGM 和战斗音效；全屏、震屏目前仍主要是偏好状态，窗口模式和震屏强度还要在对应运行时系统中继续落地。

这些不应继续塞进 `SideCombatSystem`，后续应拆到：

| 未来模块 | 计划职责 |
| --- | --- |
| `Inventory` | 材料、装备实例、掉落入包 |
| `Progression` | 等级、经验、魔剑技能树 |
| `Relationship` | 好感、支援技能等级 |
| `Hub` | 据点面板、路线确认、重刷副本 |

## 7. 打包要求

打包入口仍建议使用：

```text
assets/scenes/VisualNovelMainMenu.wt
```

打包器会从入口场景递归解析：

- `.wt` 场景。
- `.vn` 剧本。
- `.json` 数据。
- `assets/...` 资源引用。
- `{expression}` 这类模板立绘引用。

因此竖切资源不要手动复制进 `Build/assets`，应由 `.wtpack` 根据引用自动收集。

## 8. v0.8 验证记录

本轮完成后已验证：

- `Wheatear.sln` Debug x64 构建通过。
- 编辑器运行版烟测通过：据点首页、副本选择、好感关系、支援配置、设置、保存读取、黑林兽道均能启动并保持运行。
- 打包入口仍使用 `assets/scenes/VisualNovelMainMenu.wt`。
- 打包成功生成 `Builds/Windows/Player/content.wtpack`，本轮收集 118 个引用资源。
- 打包版烟测通过：默认主菜单、系统完整性页面和黑林兽道均能加载。
- 构建仍有既有 Mono/PDB 链接警告，但本轮无新增阻塞错误。

## 9. v0.9 验证记录

本轮完成后已验证：

- `Wheatear.sln` Debug x64 构建通过，0 errors；仍有既有 Mono/PDB 和导入描述符链接警告。
- 编辑器运行版烟测通过：`VerticalSliceIntro.wt`、`VerticalSliceSkillTree.wt`、`VerticalSliceEquipment.wt`、`VerticalSliceSettings.wt`、`VerticalSliceSaveLoad.wt` 均能启动并保持运行。
- 打包入口仍使用 `assets/scenes/VisualNovelMainMenu.wt`，打包成功生成 `Builds/Windows/Player/content.wtpack`，本轮收集 127 个引用资源。
- 打包版烟测通过：VN 入口、技能树、装备页、设置页和保存读取页均能加载。
- 静态检查确认 VN 场景存在 `vn:savemenu` / `vn:loadmenu` 入口和 `VN_SaveLoadPanel` 覆盖层；技能树节点、装备分页和设置滑条均存在对应命令或组件。
## 10. v0.10 战斗手感与资源序列升级记录

本轮目标是把竖切战斗从“可判定”推进到“有动作游戏反馈雏形”。

已完成：

- `SideCombatSystem` 接入攻击挥击音、命中音、命中停顿和镜头轻震。
- `SideCombatLevelComponent` 增加 hit pause 与 camera shake 运行时字段。
- `SideHitboxComponent` 增加 `HitSound / HitPause / CameraShake` 等反馈字段，并已接入场景序列化。
- `SideCombatantComponent` 增加当前视觉 clip 和 clip 计时，角色本体会按状态切换序列帧。
- `side_combat_tuning.yaml` 升级到 `0.8.0`，以中文注释暴露跳跃、重力、空连、动作时间、特效帧、音效和反馈参数。
- `tools/vertical_slice/generate_side_combat_assets.py` 现在会生成玩家、黑熊 Boss、小怪的动作序列帧，并生成 19 个基础 wav 音效。
- 竖切战斗继续使用自定义 kinematic 控制器，物理引擎只作为未来碰撞辅助候选，不作为核心跳跃/浮空求解器。

新增资源目录：

```text
assets/vertical_slice/side_combat/characters/
assets/vertical_slice/side_combat/enemies/
assets/vertical_slice/side_combat/effects/
assets/vertical_slice/side_combat/audio/
```

后续人工试玩重点：

- 如果主角跳不上敌人：优先提高 `player.jumpImpulse` 或降低 `player.gravity`。
- 如果空中 J 接不上：优先提高 `air_basic.hitStun`、`air_basic.attackerAirImpulse`，或降低 `air_basic.recovery`。
- 如果敌人被打得太飘：降低 `launcher.launchVelocity.y`、`air_chase.launchVelocity.y` 或 `reactions.bossLaunchBonus`。
- 如果打击感太轻：提高对应攻击的 `hitPause`、`cameraShake`，或替换对应 `hitSound`。
- 如果节奏太卡：先降低 `hitPause`，再检查音效文件是否过大或首次加载是否需要预热。

## 11. v0.11 编辑器调参、VN BGM 与道具栏记录

本轮目标是让剧情和战斗内容更接近“策划能在编辑器里维护”的状态。

已完成：

- VN 脚本支持 `@music` / `@bgm`，剧情推进时可切换循环 BGM。
- VN 运行时新增左上角 BGM 名称提示动画。
- 新增第一批占位 VN BGM：日常上学、异兆、异界森林、青梅被夺、吐槽假玩法、教程决意、边境旅路。
- WheatearEditor 新增 `VN Script Editor`，可管理台词、人物、表情、背景、BGM、选择项和跳转。
- WheatearEditor 新增 `Side Combat Tuning Editor`，可管理手感、战斗规则、招式、技能和 profile。
- 战斗 HUD 左侧三格改为 `SC_ItemSlot_1 / 2 / 3` 道具栏，显示 `回复药`、`凝神药剂`、`裂空爆弹` 占位图标和快捷键角标。
- 最新编辑器已重新生成并通过隐藏窗口启动烟测。

当前打包记录：

- 打包入口仍使用 `assets/scenes/VisualNovelMainMenu.wt`。
- 最近一次完整玩家包生成 `Builds/Windows/Player/content.wtpack`，收集 338 个引用资源。
- `Builds/Windows/Player/WheatearSandbox.exe` 已能启动主菜单、VN 场景和 SideCombat 场景。

## 12. v0.12 竖切流程迁移到事件脚本

本轮目标是把旧 demo 中分散在按钮、VN 选择和战斗组件里的直接流程命令，迁移到统一的 `FlowController + vertical_slice_flow.wts`。

已完成：

- 新增正式竖切流程脚本 `assets/events/vertical_slice_flow.wts`，集中管理主菜单、VN、假玩法、正式战斗、据点、技能树、装备、好感、支援、存读档、设置和章节预告的跳转。
- 竖切相关场景均新增 `FlowController` 实体，并挂载 `EventScriptComponent`，脚本路径统一指向 `assets/events/vertical_slice_flow.wts`。
- 主菜单按钮从直接 `newgame:`、`loadgame:`、`quit` 迁移为 `event:menu_new_game`、`event:menu_load_game`、`event:menu_quit`。
- 据点和各系统页面的返回 / 跳转按钮迁移为 `event:<eventName>`，页面本身不再关心真实目标场景路径。
- VN 选择项支持 `event:`，序章和假玩法转折 VN 里的战斗入口已迁移为 `event:go_fake_combat` / `go_side_combat`。
- 假玩法战斗、正式 SideCombat 和黑林兽道的胜利 / 失败命令迁移为事件脚本入口，由 `.wts` 决定等待、结算、重试或跳转。
- 静态检查确认：竖切场景按钮、VN 选择和战斗胜负命令里不再残留直接 `scene:`、`newgame:`、`loadgame:`、`quit` 旧写法。
- 静态检查确认：所有竖切 `event:*` 引用都能在 `vertical_slice_flow.wts` 中找到对应事件。

验证记录：

- `WheatearSandbox` Debug x64 构建通过，0 错误；仍有既有 Mono/PDB 链接警告。
- `WheatearEditor` Debug x64 构建通过，0 错误。
- 直接运行版烟测通过：主菜单、VN 序章、假玩法战斗、正式 SideCombat 均能启动并保持运行。
- 已重新生成打包版 `Builds/Windows/Player/content.wtpack`，本轮收集 337 个引用资源。
- 打包版内容包确认包含 `assets/events/vertical_slice_flow.wts`、`VisualNovelMainMenu.wt` 和 `SideCombatVerticalSlice.wt`。
- 打包版 `Builds/Windows/Player/WheatearSandbox.exe` 入口烟测通过。

## 13. v0.13 战斗后保存与剧情流程骨架

本轮目标是补齐事件脚本迁移的第二阶段关键闭环，并给第三阶段保留干净入口。

已完成：

- `progression:` 命令新增 `set_flag`、`clear_flag`、`set_chapter`，事件脚本可以直接设置剧情旗标和章节编号。
- `.wts` 条件新增 `last_dungeon <dungeonId>`，结算页可以根据上一场完成的副本决定重试目标。
- `fake_combat_victory` 现在会设置 `FLAG_CH01_FAKE_COMBAT_CLEARED` 并保存一号槽，再进入假玩法后的 VN。
- `tutorial_side_combat_start` 作为正式战斗教学入口，进入主线 SideCombat 前设置 `FLAG_CH02_TUTORIAL_STARTED` 并保存。
- `side_combat_victory` 现在会设置 `FLAG_CH02_TUTORIAL_CLEARED` 并保存一号槽，再进入结算页。
- `beast_path_victory` 现在会保存一号槽，再进入结算页。
- 结算页“回据点”改为 `event:result_go_hub`，会先保存再进入据点。
- 结算页“再次挑战”改为 `event:result_retry_last_dungeon`，主线战斗和黑林兽道都会回到各自副本。
- 据点“继续剧情”改为 `event:go_next_story`，会检查 `FLAG_CH02_BOSS_DEFEATED`，设置 `FLAG_CH03_PREVIEW_STARTED`，章节推进到第 3 章并保存，再进入第三章预告。

暂缓：

- 具体教程提示文本仍留在 VN / 战斗 UI 中，等教程系统稳定后再抽成 `tutorial:` 命令或教程数据表。
- 大量后续章节条件分发暂不提前写死，等第三章以后内容成型再继续迁移。

## 14. v0.14 回合制战斗竖切接入

本轮目标是在“类元气假玩法”和正式 `SideCombat` 之间加入一个可运行的回合制战斗模块，让竖切拥有第三种玩法雏形。

已完成：

- 新增运行时模块 `TurnCombat`，源码位于 `Wheatear/src/Wheatear/Modules/TurnCombat/`。
- 新增编辑器组件面板 `WheatearEditor/src/Modules/TurnCombat/TurnCombatDrawer.*`，可以编辑回合制关卡、单位数值、技能槽、UI 绑定和动画帧模板。
- `CommandBus` 新增 `turn:` 运行时玩法命令队列，UI 按钮可以触发 `turn:skill:*`、`turn:target:*`、`turn:wait`，由 `TurnCombatSystem` 消费。
- 新增场景 `assets/scenes/VerticalSliceTurnCombat.wt`，采用左下我方、右上敌方的斜向站位，底部命令栏和顶部行动顺序区参考传统 RPG / 轨迹式信息层级。
- 竖切流程脚本更新：`fake_combat_victory` 进入回合制战斗；`turn_combat_victory` 保存进度后进入 `VerticalSlicePostFake.wt`；失败则重试回合制场景。
- 新增生成脚本 `tools/vertical_slice/generate_turn_combat_assets.py`，可复现生成回合制角色序列帧和占位音频。
- 新增回合制素材目录 `assets/vertical_slice/turn_combat/`，包含背景、角色、技能图标、目标标记、行动特效、BGM 和 SFX。
- 回合制角色支持 `idle / attack / hit / down` 四类动画帧模板，运行时按行动、受击和倒下状态切换贴图。
- 回合制 BGM `turn_battle_bgm.wav` 已挂到场景 AudioSource；技能音效在释放技能时由 `TurnCombatSystem` 播放。

当前边界：

- 技能库仍是 C++ 内置表；后续建议迁到 `turn_combat_tuning.yaml`，并新增 `Turn Combat Tuning Editor`。
- 队友当前是自动行动，用于预览支援感；后续可扩展为可配置 AI 或玩家可控队伍。
- 回合制暂未接入掉落和结算页，当前作为剧情过渡战斗，胜利后回到既有竖切 VN 流程。

## 15. v0.15 运行时文本编码统一

本轮目标是修复新打包版菜单、Panel 和 VN UI 中文文本乱码，并把竖切运行时文本资产的编码规则重新落地。

已完成：

- `VisualNovelMainMenu.wt`、`VerticalSliceIntro.wt`、`VerticalSliceChapter3Preview.wt`、据点、技能树、装备、关系、支援、存读档、设置、结算和副本选择等 13 个 `.wt` 场景已从 GBK/ANSI 字节转换为 UTF-8。
- `assets/scenes`、`assets/vn`、`assets/events` 和 `assets/vertical_slice/data` 下的 `.wt`、`.vn`、`.wts`、`.json`、`.yaml` 文本资产已通过 UTF-8 解码校验。
- 运行时资产规则确认：中文文本必须以 UTF-8 保存；打包器不会对场景、VN 脚本、事件脚本或数据表做隐式转码。
- 最新玩家包重新生成到 `Builds/Windows/Player`，`content.wtpack` 收集 450 个引用资源，并确认包含主菜单、回合制战斗场景和竖切事件脚本。

验证记录：

- `WheatearSandbox` Debug x64 构建通过，0 错误。
- 打包版 `Builds/Windows/Player/WheatearSandbox.exe` 通过 8 秒隐藏窗口启动烟测。

## 16. v0.16 回合制战斗可推进修复

本轮目标是让回合制战斗面向中文玩家可读，并修复“选择技能后点击敌人无效、无法造成伤害和推进流程”的问题。

已完成：

- `TurnCombatSystem` 技能库显示文本改为中文：魔剑斩、灵素剑锋、白誓治愈、守护誓约、黑焰爆发、凝神、爪击、猛扑和暗影魔弹均有中文说明。
- 回合制运行时提示改为中文：回合开始、行动中、选择目标、魔力不足、无效目标、造成伤害、恢复、胜利和失败等消息均不再显示英文。
- `VerticalSliceTurnCombat.wt` 中角色、敌人、状态栏、技能按钮和标题默认文本改为中文。
- 修复目标点击无效：目标按钮命令从旧的 `TC_Bear`、`TC_Wolf`、`TC_Mage` 等短名改为真实战斗实体 Tag；系统端新增按目标按钮名反查战斗单位的兜底逻辑。
- 项目自有文本完成 UTF-8 统一扫描和转换：旧 GBK/ANSI 源码、编辑器面板、着色器和字体许可文本已转为 UTF-8，局部损坏中文注释改为英文注释。

验证记录：

- 项目自有文本 UTF-8 校验通过，`invalid_utf8=0`。
- `VerticalSliceTurnCombat.wt` 的 6 个 `turn:target:*` 命令均能匹配到战斗实体。
- `Wheatear.sln` Debug x64 构建通过，0 错误；仍有既有 miniaudio/PDB/导入描述符警告。
- 直接运行版 `VerticalSliceTurnCombat.wt` 通过 8 秒隐藏窗口启动烟测。
- 最新玩家包重新生成到 `Builds/Windows/Player`，`content.wtpack` 收集 450 个引用资源；打包版主入口和打包版回合制场景均通过 8 秒隐藏窗口启动烟测。

## 17. v0.17 Batch01 VN 美术资源接入

本轮目标是把已生成的 Batch01A/Batch01B VN 高清背景和立绘替换进当前竖切工程，并让新增背景进入实际剧情流程。

已完成：

- 从仓库根目录 `vn/` 接入 21 张 Batch01 PNG：`backgrounds/*.png` 共 6 张，`portraits/*.png` 共 15 张。
- 目标目录统一为 `WheatearEditor/assets/vertical_slice/vn/backgrounds/` 与 `WheatearEditor/assets/vertical_slice/vn/portraits/`，正式命名保持 Batch01 文档规范。
- 尺寸和透明度校验通过：背景为 1920x1080，立绘为 1024x1536 且带透明通道。
- `vertical_slice_intro.vn` 增加路口异常背景切换，使用 `bg_modern_schoolroad_unease.png`。
- `vertical_slice_post_fake.vn` 与 `VerticalSlicePostFake.wt` 起始背景改为 `bg_forest_after_bear.png`，对应假玩法战后森林。
- 当前竖切脚本使用的 VN 背景和表情资源均存在；精确旧名扫描没有发现旧 demo VN 资源引用。

验证记录：

- 直接运行版 `VerticalSliceIntro.wt`、`VerticalSlicePostFake.wt` 和 `VerticalSliceChapter3Preview.wt` 均通过 8 秒隐藏窗口启动烟测。
- 最新玩家包重新生成到 `Builds/Windows/Player`，`content.wtpack` 收集 452 个引用资源。
- 打包版主入口、打包版 `VerticalSliceIntro.wt` 和打包版 `VerticalSlicePostFake.wt` 均通过 8 秒隐藏窗口启动烟测。
- 包内确认包含 `bg_modern_schoolroad_unease.png`、`bg_forest_after_bear.png`、`aoba_neutral.png` 和 `mentor_neutral.png`。

## 18. v0.18 VN 音量设置接入

本轮目标是解决新游戏和 VN BGM 听感偏小，并让玩家能在 VN 设置覆盖层直接调音量。

已完成：

- 默认音量改为 `Master 85 / BGM 90 / SFX 85`，不再沿用此前偏保守的 `50 / 50 / 50`。
- `VisualNovelSystem` 在检测到 VN 设置面板时，会运行时生成主音量、BGM 音量和音效音量滑条，以及对应 `- / +` 按钮。
- VN BGM 已持续按主音量和 BGM 音量重算播放句柄音量，设置页调整后当前音乐立即生效。
- 据点设置页补齐中文音量标签，BGM / SFX 设置命令反馈改为中文。
- 相关竖切文档已更新，旧的“音量仍是偏好状态”记录改为当前实现状态。

验证记录：

- `git diff --check` 通过，只有既有文档行尾提示。
- `Wheatear.sln` Debug x64 构建通过，0 错误；仍有既有链接/PDB 警告。
- 最新玩家包重新生成到 `Builds/Windows/Player`，`content.wtpack` 收集 452 个引用资源。
- 打包版主入口和打包版 `VerticalSliceIntro.wt` 均通过 8 秒隐藏窗口启动烟测。
